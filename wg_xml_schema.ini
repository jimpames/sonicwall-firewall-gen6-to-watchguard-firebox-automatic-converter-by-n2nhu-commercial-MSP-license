# ============================================================================
# wg_xml_schema.ini
#
# Schema for the WatchGuard XML configuration profile.
# Auto-bootstrapped from reference exports; hand-curated for
# value constraints, cross-references, and cardinality refinements.
#
# Bootstrapped from: T30jpa1__1_.xml
# 
#   (C) Copyright 2026, jim ames. all rights reserved. 
#   reverse engineering my system is prohibited.
#   license: closed source, commercial use license required.
#   no hobby or one-off use- MSP only
#
#   contact: jimpames@gmail.com for licensing
#
#   $10,000 per MSP one-time
# 
#  need something else other than sonicwall to watchguard?
#  translate most anything to sonicwall with THEIR universal translator
#  then use FESv11 algebraic to translate THAT to watchguard... 
# 
# 
# ============================================================================

[meta]
name        = wg_xml_schema
version     = 1.0.0
description = WatchGuard XML element-shape rules.
              Each [element.X] section declares the validation
              rules for one element type (any element with tag X).

# ----------------------------------------------------------------------------
# profile
# ----------------------------------------------------------------------------
[element.profile]
kind        = record
# learned: 1 instance(s) in reference
allowed_fields  = abs-ipsec-action-list, abs-muvpn-list, abs-policy-list, account-list, address-group-list, alarm-action-list, alias-list, app-action-list, apps-block-profile-list, auth-domain-list, auth-group-list, base-model, device-group-list, dlp-sensor-list, dnat-list, for-version, gateway-wireless-controller, geolocation-blocking, hotspot-list, https-exception-override-list, ike-action-list, ike-policy-group-list, ike-policy-list, interface-list, ipsec-action-list, ipsec-proposal-list, l2tp-list, load-balance-list, nat-list, one-to-one-nat-list, policy-filter-list, policy-list, policy-tag-list, policy-view, probe-list, product-grade, proxy-action-list, proxy-helper-list, radius-conf-list, ras-domain-item-list, ras-server-group-list, ras-user-group-list, ras-user-list, revision-history, rs-version, schedule-list, service-list, signature-update, sslvpn-list, system-parameters, tls-profile-list, traffic-mgmt-list, user-group-list, using-cpm-profile, webblocker-global-exception-list, xml-purpose

# ----------------------------------------------------------------------------
# Engine  (occurs in: services)
# ----------------------------------------------------------------------------
[element.Engine]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# GAV  (occurs in: services)
# ----------------------------------------------------------------------------
[element.GAV]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# IAV  (occurs in: services)
# ----------------------------------------------------------------------------
[element.IAV]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# IPS  (occurs in: services)
# ----------------------------------------------------------------------------
[element.IPS]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# URI  (occurs in: connection-string)
# ----------------------------------------------------------------------------
[element.URI]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# abs-ipsec-action  (occurs in: abs-ipsec-action-list)
# ----------------------------------------------------------------------------
[element.abs-ipsec-action]
kind        = record
# learned: 1 instance(s) in reference
allowed_fields  = allow-all-traffic, description, enabled, ike-policy, ipsec-action, local-remote-pair-list, name, property

# ----------------------------------------------------------------------------
# abs-ipsec-action-list  (occurs in: profile)
# ----------------------------------------------------------------------------
[element.abs-ipsec-action-list]
kind        = container
# learned: 1 instance(s) in reference
item_element = abs-ipsec-action
allow_empty  = false

# ----------------------------------------------------------------------------
# abs-muvpn  (occurs in: abs-muvpn-list)
# ----------------------------------------------------------------------------
[element.abs-muvpn]
kind        = record
# learned: 1 instance(s) in reference
allowed_fields  = all-traffic-thru-tunnel, backup-firebox-ip, firebox-ip, ipsec-action, line-conn-mode, line-inactivity-timeout, name, passphrase, resource-addr-list, tunnel-auth

# ----------------------------------------------------------------------------
# abs-muvpn-list  (occurs in: profile)
# ----------------------------------------------------------------------------
[element.abs-muvpn-list]
kind        = container
# learned: 1 instance(s) in reference
item_element = abs-muvpn
allow_empty  = false

# ----------------------------------------------------------------------------
# abs-policy  (occurs in: abs-policy-list)
# ----------------------------------------------------------------------------
[element.abs-policy]
kind        = record
# learned: 15 instance(s) in reference
required_fields = enabled, firewall, from-alias-list, name, policy-list, property, reject-action, service, settings, to-alias-list, type
allowed_fields  = description, enabled, firewall, from-alias-list, muvpn-users-list, name, policy-list, policy-nat, property, reject-action, service, settings, to-alias-list, traffic-type, type

# ----------------------------------------------------------------------------
# abs-policy-list  (occurs in: profile)
# ----------------------------------------------------------------------------
[element.abs-policy-list]
kind        = container
# learned: 1 instance(s) in reference
item_element = abs-policy
allow_empty  = false

# ----------------------------------------------------------------------------
# account-list  (occurs in: profile)
# ----------------------------------------------------------------------------
[element.account-list]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# acl  (occurs in: sip, h323)
# ----------------------------------------------------------------------------
[element.acl]
kind        = record
# learned: 2 instance(s) in reference
allowed_fields  = addresses, enabled

# ----------------------------------------------------------------------------
# action  (occurs in: timeout, max-total-length, max-line-length)
# ----------------------------------------------------------------------------
[element.action]
kind        = ambiguous
# learned: 122 instance(s) in reference

# ----------------------------------------------------------------------------
# action-type  (occurs in: remote-action)
# ----------------------------------------------------------------------------
[element.action-type]
kind        = leaf
# learned: 55 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# active  (occurs in: sslfilter, ocsp)
# ----------------------------------------------------------------------------
[element.active]
kind        = leaf
# learned: 21 instance(s) in reference
value_type      = string
allowed_values  = OCSP_NO, OCSP_YES, SSL_NO, SSL_YES
allow_empty     = false

# ----------------------------------------------------------------------------
# addr-family  (occurs in: ike-policy)
# ----------------------------------------------------------------------------
[element.addr-family]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# addr-group-member  (occurs in: address-group)
# ----------------------------------------------------------------------------
[element.addr-group-member]
kind        = container
# learned: 14 instance(s) in reference
item_element = member
allow_empty  = false

# ----------------------------------------------------------------------------
# addr-grp-name  (occurs in: ras-user-group)
# ----------------------------------------------------------------------------
[element.addr-grp-name]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# addr-mgmt  (occurs in: ras-user-group)
# ----------------------------------------------------------------------------
[element.addr-mgmt]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# addr-name  (occurs in: member)
# ----------------------------------------------------------------------------
[element.addr-name]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# addr-type  (occurs in: member)
# ----------------------------------------------------------------------------
[element.addr-type]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# address  (occurs in: alias-member, redirect, quarantine)
# ----------------------------------------------------------------------------
[element.address]
kind        = leaf
# learned: 26 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# address-group  (occurs in: address-group-list)
# ----------------------------------------------------------------------------
[element.address-group]
kind        = record
# learned: 14 instance(s) in reference
required_fields = addr-group-member, name, property
allowed_fields  = addr-group-member, description, name, property

# ----------------------------------------------------------------------------
# address-group-list  (occurs in: profile)
# ----------------------------------------------------------------------------
[element.address-group-list]
kind        = container
# learned: 1 instance(s) in reference
item_element = address-group
allow_empty  = false

# ----------------------------------------------------------------------------
# addresses  (occurs in: envelope, acl)
# ----------------------------------------------------------------------------
[element.addresses]
kind        = record
# learned: 6 instance(s) in reference
allowed_fields  = fallthrough, from, maximum-length, maximum-to, to

# ----------------------------------------------------------------------------
# agent-ip-list  (occurs in: terminal-service)
# ----------------------------------------------------------------------------
[element.agent-ip-list]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# alarm  (occurs in: rule, fallthrough, action)
# ----------------------------------------------------------------------------
[element.alarm]
kind        = leaf
# learned: 239 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# alarm-action  (occurs in: alarms)
# ----------------------------------------------------------------------------
[element.alarm-action]
kind        = record
# learned: 55 instance(s) in reference
required_fields = enable, launch-interval, name, remote-action, remote-enable, repeat-count, trap-enable
allowed_fields  = block-ip-enable, block-ip-interval, block-ip-violation, condition-list, email-addr, email-enable, email-from, email-subject, enable, event-type, interval, launch-interval, name, op, property, protocol, remote-action, remote-enable, repeat-count, severity, trap-enable

# ----------------------------------------------------------------------------
# alarm-action-list  (occurs in: profile)
# ----------------------------------------------------------------------------
[element.alarm-action-list]
kind        = container
# learned: 1 instance(s) in reference
item_element = alarm-action-sub-list
allow_empty  = false

# ----------------------------------------------------------------------------
# alarm-action-sub-list  (occurs in: alarm-action-list)
# ----------------------------------------------------------------------------
[element.alarm-action-sub-list]
kind        = record
# learned: 8 instance(s) in reference
required_fields = alarm-action-type, alarms
allowed_fields  = alarm-action-type, alarms

# ----------------------------------------------------------------------------
# alarm-action-type  (occurs in: alarm-action-sub-list)
# ----------------------------------------------------------------------------
[element.alarm-action-type]
kind        = leaf
# learned: 8 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# alarms  (occurs in: alarm-action-sub-list)
# ----------------------------------------------------------------------------
[element.alarms]
kind        = container
# learned: 8 instance(s) in reference
item_element = alarm-action
allow_empty  = false

# ----------------------------------------------------------------------------
# algorithm  (occurs in: multi-wan, nat)
# ----------------------------------------------------------------------------
[element.algorithm]
kind        = leaf
# learned: 2 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# alias  (occurs in: alias-list, from-alias-list, to-alias-list)
# ----------------------------------------------------------------------------
[element.alias]
kind        = ambiguous
# learned: 112 instance(s) in reference

# ----------------------------------------------------------------------------
# alias-list  (occurs in: profile)
# ----------------------------------------------------------------------------
[element.alias-list]
kind        = container
# learned: 1 instance(s) in reference
item_element = alias
allow_empty  = false

# ----------------------------------------------------------------------------
# alias-member  (occurs in: alias-member-list)
# ----------------------------------------------------------------------------
[element.alias-member]
kind        = record
# learned: 62 instance(s) in reference
required_fields = type
allowed_fields  = address, alias-name, device, interface, type, user

# ----------------------------------------------------------------------------
# alias-member-list  (occurs in: alias)
# ----------------------------------------------------------------------------
[element.alias-member-list]
kind        = container
# learned: 49 instance(s) in reference
item_element = alias-member
allow_empty  = false

# ----------------------------------------------------------------------------
# alias-name  (occurs in: alias-member)
# ----------------------------------------------------------------------------
[element.alias-name]
kind        = leaf
# learned: 41 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# all-icmp-errors  (occurs in: icmp-error-handling)
# ----------------------------------------------------------------------------
[element.all-icmp-errors]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# all-traffic-thru-tunnel  (occurs in: abs-muvpn)
# ----------------------------------------------------------------------------
[element.all-traffic-thru-tunnel]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# allow  (occurs in: rule, fallthrough, error)
# ----------------------------------------------------------------------------
[element.allow]
kind        = leaf
# learned: 1473 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# allow-all-traffic  (occurs in: abs-ipsec-action)
# ----------------------------------------------------------------------------
[element.allow-all-traffic]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# allow-list  (occurs in: app-action)
# ----------------------------------------------------------------------------
[element.allow-list]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# allow-non-ssl  (occurs in: tls-profile, https)
# ----------------------------------------------------------------------------
[element.allow-non-ssl]
kind        = leaf
# learned: 5 instance(s) in reference
value_type      = string
allowed_values  = false, true
allow_empty     = false

# ----------------------------------------------------------------------------
# anonymize  (occurs in: header)
# ----------------------------------------------------------------------------
[element.anonymize]
kind        = container
# learned: 7 instance(s) in reference
item_element = rules
allow_empty  = false

# ----------------------------------------------------------------------------
# anti-replay-window  (occurs in: ipsec-proposal)
# ----------------------------------------------------------------------------
[element.anti-replay-window]
kind        = leaf
# learned: 11 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# anti-scan  (occurs in: physical-if)
# ----------------------------------------------------------------------------
[element.anti-scan]
kind        = leaf
# learned: 5 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# anti-spoof  (occurs in: physical-if)
# ----------------------------------------------------------------------------
[element.anti-spoof]
kind        = leaf
# learned: 5 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# anti-virus  (occurs in: http, ftp, smtp)
# ----------------------------------------------------------------------------
[element.anti-virus]
kind        = record
# learned: 22 instance(s) in reference
required_fields = buffer-size, enabled, error, scan-limit, virus-found
allowed_fields  = buffer-size, enabled, encrypted, error, scan-limit, too-large, virus-found

# ----------------------------------------------------------------------------
# antivirus  (occurs in: system-parameters)
# ----------------------------------------------------------------------------
[element.antivirus]
kind        = container
# learned: 1 instance(s) in reference
item_element = scan-mode
allow_empty  = false

# ----------------------------------------------------------------------------
# any  (occurs in: source, destination)
# ----------------------------------------------------------------------------
[element.any]
kind        = leaf
# learned: 4 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# ap-list  (occurs in: gateway-wireless-controller)
# ----------------------------------------------------------------------------
[element.ap-list]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# apop  (occurs in: protocol)
# ----------------------------------------------------------------------------
[element.apop]
kind        = record
# learned: 4 instance(s) in reference
allowed_fields  = allow, log

# ----------------------------------------------------------------------------
# app  (occurs in: block-list)
# ----------------------------------------------------------------------------
[element.app]
kind        = record
# learned: 2 instance(s) in reference
allowed_fields  = app_id, cat_id, name

# ----------------------------------------------------------------------------
# app-action  (occurs in: app-action-list)
# ----------------------------------------------------------------------------
[element.app-action]
kind        = record
# learned: 1 instance(s) in reference
allowed_fields  = allow-list, block-list, fallthrough, name, property

# ----------------------------------------------------------------------------
# app-action-list  (occurs in: profile)
# ----------------------------------------------------------------------------
[element.app-action-list]
kind        = container
# learned: 1 instance(s) in reference
item_element = app-action
allow_empty  = false

# ----------------------------------------------------------------------------
# app_id  (occurs in: app)
# ----------------------------------------------------------------------------
[element.app_id]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# apply-dnat-all-traffic  (occurs in: settings)
# ----------------------------------------------------------------------------
[element.apply-dnat-all-traffic]
kind        = leaf
# learned: 15 instance(s) in reference
value_type      = string
allowed_values  = false
allow_empty     = false

# ----------------------------------------------------------------------------
# apply-dnat-global  (occurs in: settings)
# ----------------------------------------------------------------------------
[element.apply-dnat-global]
kind        = leaf
# learned: 15 instance(s) in reference
value_type      = string
allowed_values  = false, true
allow_empty     = false

# ----------------------------------------------------------------------------
# apply-one-to-one-nat-rules  (occurs in: settings)
# ----------------------------------------------------------------------------
[element.apply-one-to-one-nat-rules]
kind        = leaf
# learned: 15 instance(s) in reference
value_type      = string
allowed_values  = false, true
allow_empty     = false

# ----------------------------------------------------------------------------
# apps  (occurs in: sigeng)
# ----------------------------------------------------------------------------
[element.apps]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# apps-block-profile  (occurs in: http, outgoing)
# ----------------------------------------------------------------------------
[element.apps-block-profile]
kind        = leaf
# learned: 9 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# apps-block-profile-list  (occurs in: profile)
# ----------------------------------------------------------------------------
[element.apps-block-profile-list]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# apt  (occurs in: system-parameters)
# ----------------------------------------------------------------------------
[element.apt]
kind        = record
# learned: 1 instance(s) in reference
allowed_fields  = enabled, high, log-clean, low, medium

# ----------------------------------------------------------------------------
# apt-enabled  (occurs in: ftp, http)
# ----------------------------------------------------------------------------
[element.apt-enabled]
kind        = leaf
# learned: 2 instance(s) in reference
value_type      = string
allowed_values  = false
allow_empty     = false

# ----------------------------------------------------------------------------
# archives  (occurs in: sigeng)
# ----------------------------------------------------------------------------
[element.archives]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# auth  (occurs in: esmtp-options, encryption, protocol)
# ----------------------------------------------------------------------------
[element.auth]
kind        = record
# learned: 12 instance(s) in reference
allowed_fields  = fallthrough, rule
repeating_fields = rule

# ----------------------------------------------------------------------------
# auth-algm  (occurs in: member)
# ----------------------------------------------------------------------------
[element.auth-algm]
kind        = leaf
# learned: 18 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# auth-domain  (occurs in: auth-domain-list, ras-user-group, item)
# ----------------------------------------------------------------------------
[element.auth-domain]
kind        = ambiguous
# learned: 4 instance(s) in reference

# ----------------------------------------------------------------------------
# auth-domain-list  (occurs in: ras-user-group, profile)
# ----------------------------------------------------------------------------
[element.auth-domain-list]
kind        = container
# learned: 2 instance(s) in reference
item_element = auth-domain
allow_empty  = false

# ----------------------------------------------------------------------------
# auth-enabled  (occurs in: protocol)
# ----------------------------------------------------------------------------
[element.auth-enabled]
kind        = leaf
# learned: 4 instance(s) in reference
value_type      = string
allowed_values  = true
allow_empty     = false

# ----------------------------------------------------------------------------
# auth-global-setting  (occurs in: system-parameters)
# ----------------------------------------------------------------------------
[element.auth-global-setting]
kind        = record
# learned: 1 instance(s) in reference
allowed_fields  = auth-user-session-timeout, auth-user-timeout, firebox-user-case-sensitivity, mgmt-user-idle-timeout, mgmt-user-session-timeout, redirect-url

# ----------------------------------------------------------------------------
# auth-group  (occurs in: auth-group-list)
# ----------------------------------------------------------------------------
[element.auth-group]
kind        = record
# learned: 2 instance(s) in reference
allowed_fields  = auth-group-item, description, name, property

# ----------------------------------------------------------------------------
# auth-group-item  (occurs in: auth-group)
# ----------------------------------------------------------------------------
[element.auth-group-item]
kind        = container
# learned: 1 instance(s) in reference
item_element = item
allow_empty  = false

# ----------------------------------------------------------------------------
# auth-group-list  (occurs in: profile)
# ----------------------------------------------------------------------------
[element.auth-group-list]
kind        = container
# learned: 1 instance(s) in reference
item_element = auth-group
allow_empty  = false

# ----------------------------------------------------------------------------
# auth-key-length  (occurs in: member)
# ----------------------------------------------------------------------------
[element.auth-key-length]
kind        = leaf
# learned: 11 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# auth-method  (occurs in: member, tunnel-auth)
# ----------------------------------------------------------------------------
[element.auth-method]
kind        = leaf
# learned: 7 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# auth-user-session-timeout  (occurs in: auth-global-setting)
# ----------------------------------------------------------------------------
[element.auth-user-session-timeout]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# auth-user-timeout  (occurs in: auth-global-setting)
# ----------------------------------------------------------------------------
[element.auth-user-timeout]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# authentication  (occurs in: explicit)
# ----------------------------------------------------------------------------
[element.authentication]
kind        = container
# learned: 1 instance(s) in reference
item_element = enabled
allow_empty  = false

# ----------------------------------------------------------------------------
# authorization  (occurs in: header)
# ----------------------------------------------------------------------------
[element.authorization]
kind        = container
# learned: 7 instance(s) in reference
item_element = rules
allow_empty  = false

# ----------------------------------------------------------------------------
# auto-block-enabled  (occurs in: settings)
# ----------------------------------------------------------------------------
[element.auto-block-enabled]
kind        = leaf
# learned: 15 instance(s) in reference
value_type      = string
allowed_values  = false
allow_empty     = false

# ----------------------------------------------------------------------------
# auto-blocked-duration  (occurs in: misc-global-setting)
# ----------------------------------------------------------------------------
[element.auto-blocked-duration]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# auto-blocked-enabled  (occurs in: blocked-ports)
# ----------------------------------------------------------------------------
[element.auto-blocked-enabled]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# auto-firmware-update  (occurs in: global-settings)
# ----------------------------------------------------------------------------
[element.auto-firmware-update]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# auto-interval  (occurs in: signature-update)
# ----------------------------------------------------------------------------
[element.auto-interval]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# auto-negotiation  (occurs in: physical-if)
# ----------------------------------------------------------------------------
[element.auto-negotiation]
kind        = leaf
# learned: 5 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# auto-order-enabled  (occurs in: misc-global-setting)
# ----------------------------------------------------------------------------
[element.auto-order-enabled]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# auto-start  (occurs in: ike-policy)
# ----------------------------------------------------------------------------
[element.auto-start]
kind        = leaf
# learned: 3 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# auto-start-tunnel  (occurs in: vpn-global-setting)
# ----------------------------------------------------------------------------
[element.auto-start-tunnel]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# autodetect  (occurs in: content-checking)
# ----------------------------------------------------------------------------
[element.autodetect]
kind        = leaf
# learned: 10 instance(s) in reference
value_type      = string
allowed_values  = true
allow_empty     = false

# ----------------------------------------------------------------------------
# av  (occurs in: sigeng)
# ----------------------------------------------------------------------------
[element.av]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# backlog  (occurs in: pipelining)
# ----------------------------------------------------------------------------
[element.backlog]
kind        = record
# learned: 7 instance(s) in reference
required_fields = action, value
allowed_fields  = action, value

# ----------------------------------------------------------------------------
# backup-firebox-ip  (occurs in: abs-muvpn)
# ----------------------------------------------------------------------------
[element.backup-firebox-ip]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# backup-log-server-list  (occurs in: common-logging)
# ----------------------------------------------------------------------------
[element.backup-log-server-list]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# backup-server-list  (occurs in: dvcp-client)
# ----------------------------------------------------------------------------
[element.backup-server-list]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# bandwidth  (occurs in: performance-log)
# ----------------------------------------------------------------------------
[element.bandwidth]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# banner-name  (occurs in: masquerading)
# ----------------------------------------------------------------------------
[element.banner-name]
kind        = leaf
# learned: 4 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# base-model  (occurs in: profile)
# ----------------------------------------------------------------------------
[element.base-model]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# binarymime  (occurs in: esmtp-options)
# ----------------------------------------------------------------------------
[element.binarymime]
kind        = leaf
# learned: 4 instance(s) in reference
value_type      = string
allowed_values  = false
allow_empty     = false

# ----------------------------------------------------------------------------
# binhex  (occurs in: content-checking)
# ----------------------------------------------------------------------------
[element.binhex]
kind        = leaf
# learned: 8 instance(s) in reference
value_type      = string
allowed_values  = false, true
allow_empty     = false

# ----------------------------------------------------------------------------
# block  (occurs in: action, reputation-authority)
# ----------------------------------------------------------------------------
[element.block]
kind        = record
# learned: 5 instance(s) in reference
allowed_fields  = alarm, enable, log, threshold

# ----------------------------------------------------------------------------
# block-ip-enable  (occurs in: alarm-action)
# ----------------------------------------------------------------------------
[element.block-ip-enable]
kind        = leaf
# learned: 49 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# block-ip-interval  (occurs in: alarm-action)
# ----------------------------------------------------------------------------
[element.block-ip-interval]
kind        = leaf
# learned: 3 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# block-ip-violation  (occurs in: alarm-action)
# ----------------------------------------------------------------------------
[element.block-ip-violation]
kind        = leaf
# learned: 3 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# block-list  (occurs in: app-action)
# ----------------------------------------------------------------------------
[element.block-list]
kind        = container
# learned: 1 instance(s) in reference
item_element = app
allow_empty  = false

# ----------------------------------------------------------------------------
# block-notification  (occurs in: physical-if)
# ----------------------------------------------------------------------------
[element.block-notification]
kind        = leaf
# learned: 5 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# block-spoof-enabled  (occurs in: misc-global-setting)
# ----------------------------------------------------------------------------
[element.block-spoof-enabled]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# blocked-ip-exception-list  (occurs in: system-parameters)
# ----------------------------------------------------------------------------
[element.blocked-ip-exception-list]
kind        = container
# learned: 1 instance(s) in reference
item_element = exception-ip
allow_empty  = false

# ----------------------------------------------------------------------------
# blocked-ip-list  (occurs in: system-parameters)
# ----------------------------------------------------------------------------
[element.blocked-ip-list]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# blocked-list-enabled  (occurs in: spyware)
# ----------------------------------------------------------------------------
[element.blocked-list-enabled]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# blocked-port  (occurs in: blocked-port-list)
# ----------------------------------------------------------------------------
[element.blocked-port]
kind        = leaf
# learned: 13 instance(s) in reference
value_type      = int
allow_empty     = false

# ----------------------------------------------------------------------------
# blocked-port-list  (occurs in: blocked-ports)
# ----------------------------------------------------------------------------
[element.blocked-port-list]
kind        = container
# learned: 1 instance(s) in reference
item_element = blocked-port
allow_empty  = false

# ----------------------------------------------------------------------------
# blocked-ports  (occurs in: system-parameters)
# ----------------------------------------------------------------------------
[element.blocked-ports]
kind        = record
# learned: 1 instance(s) in reference
allowed_fields  = alarm, auto-blocked-enabled, blocked-port-list, log

# ----------------------------------------------------------------------------
# body  (occurs in: response, encryption)
# ----------------------------------------------------------------------------
[element.body]
kind        = container
# learned: 11 instance(s) in reference
item_element = content-type
allow_empty  = true

# ----------------------------------------------------------------------------
# botnet-detection  (occurs in: system-parameters)
# ----------------------------------------------------------------------------
[element.botnet-detection]
kind        = container
# learned: 1 instance(s) in reference
item_element = enabled
allow_empty  = false

# ----------------------------------------------------------------------------
# buffer-size  (occurs in: anti-virus)
# ----------------------------------------------------------------------------
[element.buffer-size]
kind        = leaf
# learned: 22 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# bulk  (occurs in: spam)
# ----------------------------------------------------------------------------
[element.bulk]
kind        = record
# learned: 10 instance(s) in reference
required_fields = log, replace
allowed_fields  = log, replace

# ----------------------------------------------------------------------------
# bypass  (occurs in: filter, reputation-authority)
# ----------------------------------------------------------------------------
[element.bypass]
kind        = record
# learned: 13 instance(s) in reference
allowed_fields  = alarm, enable, log, threshold

# ----------------------------------------------------------------------------
# ca-cert  (occurs in: dvcp-client)
# ----------------------------------------------------------------------------
[element.ca-cert]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# cache-size  (occurs in: webblocker, ctspam)
# ----------------------------------------------------------------------------
[element.cache-size]
kind        = leaf
# learned: 3 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# capa  (occurs in: protocol)
# ----------------------------------------------------------------------------
[element.capa]
kind        = record
# learned: 4 instance(s) in reference
allowed_fields  = deny, log

# ----------------------------------------------------------------------------
# card-id  (occurs in: route-entry)
# ----------------------------------------------------------------------------
[element.card-id]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# cat_id  (occurs in: app)
# ----------------------------------------------------------------------------
[element.cat_id]
kind        = leaf
# learned: 2 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# categories  (occurs in: webblocker)
# ----------------------------------------------------------------------------
[element.categories]
kind        = record
# learned: 2 instance(s) in reference
allowed_fields  = fallthrough, rule
repeating_fields = rule

# ----------------------------------------------------------------------------
# ccl-name  (occurs in: content-rule)
# ----------------------------------------------------------------------------
[element.ccl-name]
kind        = leaf
# learned: 9 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# cert-list  (occurs in: ui-pm)
# ----------------------------------------------------------------------------
[element.cert-list]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# chunking  (occurs in: esmtp-options)
# ----------------------------------------------------------------------------
[element.chunking]
kind        = leaf
# learned: 4 instance(s) in reference
value_type      = string
allowed_values  = false
allow_empty     = false

# ----------------------------------------------------------------------------
# clean  (occurs in: spam)
# ----------------------------------------------------------------------------
[element.clean]
kind        = record
# learned: 10 instance(s) in reference
required_fields = allow, log
allowed_fields  = allow, log

# ----------------------------------------------------------------------------
# client  (occurs in: sslfilter)
# ----------------------------------------------------------------------------
[element.client]
kind        = record
# learned: 13 instance(s) in reference
required_fields = ecdhe
allowed_fields  = ecdhe, type

# ----------------------------------------------------------------------------
# client-fallthrough-option  (occurs in: transport)
# ----------------------------------------------------------------------------
[element.client-fallthrough-option]
kind        = leaf
# learned: 4 instance(s) in reference
value_type      = string
allowed_values  = may
allow_empty     = false

# ----------------------------------------------------------------------------
# client-id  (occurs in: dvcp-client)
# ----------------------------------------------------------------------------
[element.client-id]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# commands  (occurs in: request)
# ----------------------------------------------------------------------------
[element.commands]
kind        = record
# learned: 5 instance(s) in reference
required_fields = fallthrough
allowed_fields  = fallthrough, rule
repeating_fields = rule

# ----------------------------------------------------------------------------
# comment  (occurs in: revision)
# ----------------------------------------------------------------------------
[element.comment]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# common-logging  (occurs in: system-parameters)
# ----------------------------------------------------------------------------
[element.common-logging]
kind        = record
# learned: 1 instance(s) in reference
allowed_fields  = backup-log-server-list, enabled, encryption, log-server

# ----------------------------------------------------------------------------
# compliance-check  (occurs in: policy, settings)
# ----------------------------------------------------------------------------
[element.compliance-check]
kind        = leaf
# learned: 16 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# condition  (occurs in: condition-list)
# ----------------------------------------------------------------------------
[element.condition]
kind        = record
# learned: 14 instance(s) in reference
required_fields = condition-op, counter-id, instance-name, threshold
allowed_fields  = condition-op, counter-id, instance-name, threshold

# ----------------------------------------------------------------------------
# condition-list  (occurs in: alarm-action)
# ----------------------------------------------------------------------------
[element.condition-list]
kind        = container
# learned: 2 instance(s) in reference
item_element = condition
allow_empty  = false

# ----------------------------------------------------------------------------
# condition-op  (occurs in: condition)
# ----------------------------------------------------------------------------
[element.condition-op]
kind        = leaf
# learned: 14 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# confirmed  (occurs in: spam)
# ----------------------------------------------------------------------------
[element.confirmed]
kind        = record
# learned: 10 instance(s) in reference
required_fields = log
allowed_fields  = deny, log, replace

# ----------------------------------------------------------------------------
# connect  (occurs in: explicit)
# ----------------------------------------------------------------------------
[element.connect]
kind        = container
# learned: 1 instance(s) in reference
item_element = ports
allow_empty  = false

# ----------------------------------------------------------------------------
# connection-rate  (occurs in: policy, settings)
# ----------------------------------------------------------------------------
[element.connection-rate]
kind        = leaf
# learned: 31 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# connection-rate-alarm  (occurs in: policy, settings)
# ----------------------------------------------------------------------------
[element.connection-rate-alarm]
kind        = leaf
# learned: 7 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# connection-string  (occurs in: ctspam)
# ----------------------------------------------------------------------------
[element.connection-string]
kind        = record
# learned: 1 instance(s) in reference
allowed_fields  = URI, proactive-patterns-enabled, proxy-access, proxy-auth, proxy-password, proxy-port, proxy-server-address, proxy-user-domain, proxy-user-name, server-address, temp-dir, vod-enabled

# ----------------------------------------------------------------------------
# connection-string-override  (occurs in: ctspam)
# ----------------------------------------------------------------------------
[element.connection-string-override]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# content-actions  (occurs in: fallthrough)
# ----------------------------------------------------------------------------
[element.content-actions]
kind        = record
# learned: 1 instance(s) in reference
allowed_fields  = dest-port, tls-dest-port

# ----------------------------------------------------------------------------
# content-checking  (occurs in: message)
# ----------------------------------------------------------------------------
[element.content-checking]
kind        = record
# learned: 10 instance(s) in reference
required_fields = autodetect, content-types, file-patterns
allowed_fields  = autodetect, binhex, content-types, deny-message, file-patterns, messages, uuencode

# ----------------------------------------------------------------------------
# content-rule  (occurs in: content-rule-list)
# ----------------------------------------------------------------------------
[element.content-rule]
kind        = record
# learned: 9 instance(s) in reference
required_fields = ccl-name, enabled
allowed_fields  = ccl-name, enabled

# ----------------------------------------------------------------------------
# content-rule-list  (occurs in: dlp-sensor)
# ----------------------------------------------------------------------------
[element.content-rule-list]
kind        = container
# learned: 2 instance(s) in reference
item_element = content-rule
allow_empty  = false

# ----------------------------------------------------------------------------
# content-rules  (occurs in: http)
# ----------------------------------------------------------------------------
[element.content-rules]
kind        = container
# learned: 1 instance(s) in reference
item_element = fallthrough
allow_empty  = false

# ----------------------------------------------------------------------------
# content-type  (occurs in: header, body)
# ----------------------------------------------------------------------------
[element.content-type]
kind        = container
# learned: 14 instance(s) in reference
item_element = rules
allow_empty  = false

# ----------------------------------------------------------------------------
# content-types  (occurs in: content-checking)
# ----------------------------------------------------------------------------
[element.content-types]
kind        = record
# learned: 10 instance(s) in reference
required_fields = fallthrough
allowed_fields  = fallthrough, rule
repeating_fields = rule

# ----------------------------------------------------------------------------
# cookies  (occurs in: header)
# ----------------------------------------------------------------------------
[element.cookies]
kind        = container
# learned: 7 instance(s) in reference
item_element = domains
allow_empty  = false

# ----------------------------------------------------------------------------
# counter-id  (occurs in: condition)
# ----------------------------------------------------------------------------
[element.counter-id]
kind        = leaf
# learned: 14 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# critical  (occurs in: threat-level)
# ----------------------------------------------------------------------------
[element.critical]
kind        = record
# learned: 1 instance(s) in reference
allowed_fields  = action, alarm, log

# ----------------------------------------------------------------------------
# ctspam  (occurs in: proxy-helper)
# ----------------------------------------------------------------------------
[element.ctspam]
kind        = record
# learned: 1 instance(s) in reference
allowed_fields  = cache-size, connection-string, connection-string-override, max-scan-file-size, max-threads, spam-threshold, vod-activated, vod-max-scan-file-size, vod-threshold

# ----------------------------------------------------------------------------
# day-of-week  (occurs in: member)
# ----------------------------------------------------------------------------
[element.day-of-week]
kind        = leaf
# learned: 6 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# ddns  (occurs in: external-if)
# ----------------------------------------------------------------------------
[element.ddns]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# ddns-service-list  (occurs in: system-parameters)
# ----------------------------------------------------------------------------
[element.ddns-service-list]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# debug  (occurs in: smtp, dvcp-client, lsd)
# ----------------------------------------------------------------------------
[element.debug]
kind        = leaf
# learned: 6 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# debug-level  (occurs in: log-conf)
# ----------------------------------------------------------------------------
[element.debug-level]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# default-gateway  (occurs in: physical-if)
# ----------------------------------------------------------------------------
[element.default-gateway]
kind        = leaf
# learned: 5 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# denied-codecs  (occurs in: sip, h323)
# ----------------------------------------------------------------------------
[element.denied-codecs]
kind        = container
# learned: 2 instance(s) in reference
item_element = fallthrough
allow_empty  = false

# ----------------------------------------------------------------------------
# deny  (occurs in: action, rule, fallthrough)
# ----------------------------------------------------------------------------
[element.deny]
kind        = leaf
# learned: 221 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# deny-message  (occurs in: http, content-checking)
# ----------------------------------------------------------------------------
[element.deny-message]
kind        = leaf
# learned: 13 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# depth  (occurs in: sigeng)
# ----------------------------------------------------------------------------
[element.depth]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# description  (occurs in: service, exception-ip, alias)
# ----------------------------------------------------------------------------
[element.description]
kind        = leaf
# learned: 250 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# dest-address  (occurs in: route-entry)
# ----------------------------------------------------------------------------
[element.dest-address]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# dest-port  (occurs in: content-actions)
# ----------------------------------------------------------------------------
[element.dest-port]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = int
allow_empty     = false

# ----------------------------------------------------------------------------
# destination  (occurs in: filter)
# ----------------------------------------------------------------------------
[element.destination]
kind        = record
# learned: 2 instance(s) in reference
allowed_fields  = any, type

# ----------------------------------------------------------------------------
# device  (occurs in: alias-member)
# ----------------------------------------------------------------------------
[element.device]
kind        = leaf
# learned: 3 instance(s) in reference
value_type      = string
allowed_values  = Any
allow_empty     = false

# ----------------------------------------------------------------------------
# device-conf  (occurs in: system-parameters)
# ----------------------------------------------------------------------------
[element.device-conf]
kind        = record
# learned: 1 instance(s) in reference
allowed_fields  = domain-name, email, for-model, location, system-contact, system-name, time-zone

# ----------------------------------------------------------------------------
# device-group  (occurs in: device-group-list)
# ----------------------------------------------------------------------------
[element.device-group]
kind        = record
# learned: 4 instance(s) in reference
allowed_fields  = description, name, property

# ----------------------------------------------------------------------------
# device-group-list  (occurs in: profile)
# ----------------------------------------------------------------------------
[element.device-group-list]
kind        = container
# learned: 1 instance(s) in reference
item_element = device-group
allow_empty  = false

# ----------------------------------------------------------------------------
# dh-group  (occurs in: member, ipsec-action)
# ----------------------------------------------------------------------------
[element.dh-group]
kind        = leaf
# learned: 8 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# dhcp-client  (occurs in: external-if)
# ----------------------------------------------------------------------------
[element.dhcp-client]
kind        = record
# learned: 1 instance(s) in reference
allowed_fields  = force-renew, lease-time

# ----------------------------------------------------------------------------
# dhcp-server  (occurs in: physical-if)
# ----------------------------------------------------------------------------
[element.dhcp-server]
kind        = container
# learned: 2 instance(s) in reference
item_element = server-type
allow_empty  = false

# ----------------------------------------------------------------------------
# diag-log-level  (occurs in: proxy-action, proxy-helper)
# ----------------------------------------------------------------------------
[element.diag-log-level]
kind        = leaf
# learned: 4 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# direction  (occurs in: local-remote-pair)
# ----------------------------------------------------------------------------
[element.direction]
kind        = leaf
# learned: 3 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# dlp  (occurs in: system-parameters)
# ----------------------------------------------------------------------------
[element.dlp]
kind        = record
# learned: 1 instance(s) in reference
allowed_fields  = dlp-enabled, log-dlp-stats, log-dlp-stats-interval, max-extracted-text-size, text-extraction-enabled

# ----------------------------------------------------------------------------
# dlp-action  (occurs in: filter)
# ----------------------------------------------------------------------------
[element.dlp-action]
kind        = record
# learned: 2 instance(s) in reference
allowed_fields  = action, alarm, log, smtp-action

# ----------------------------------------------------------------------------
# dlp-enabled  (occurs in: dlp)
# ----------------------------------------------------------------------------
[element.dlp-enabled]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# dlp-sensor  (occurs in: dlp-sensor-list)
# ----------------------------------------------------------------------------
[element.dlp-sensor]
kind        = record
# learned: 2 instance(s) in reference
allowed_fields  = content-rule-list, description, error-action, filter-list, name, property, scan-limit, too-large-action, unscannable-action

# ----------------------------------------------------------------------------
# dlp-sensor-list  (occurs in: ftp, http, profile)
# ----------------------------------------------------------------------------
[element.dlp-sensor-list]
kind        = container
# learned: 3 instance(s) in reference
item_element = dlp-sensor
allow_empty  = true

# ----------------------------------------------------------------------------
# dnat  (occurs in: dnat-list)
# ----------------------------------------------------------------------------
[element.dnat]
kind        = record
# learned: 3 instance(s) in reference
allowed_fields  = from-alias, to-alias

# ----------------------------------------------------------------------------
# dnat-enabled  (occurs in: local-remote-pair)
# ----------------------------------------------------------------------------
[element.dnat-enabled]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# dnat-list  (occurs in: profile)
# ----------------------------------------------------------------------------
[element.dnat-list]
kind        = container
# learned: 1 instance(s) in reference
item_element = dnat
allow_empty  = false

# ----------------------------------------------------------------------------
# dnat-src-ip  (occurs in: settings, local-remote-pair)
# ----------------------------------------------------------------------------
[element.dnat-src-ip]
kind        = leaf
# learned: 16 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# dns  (occurs in: proxy-action)
# ----------------------------------------------------------------------------
[element.dns]
kind        = record
# learned: 2 instance(s) in reference
allowed_fields  = ips, request, response, transaction, zones

# ----------------------------------------------------------------------------
# dns-entry  (occurs in: dns-server-list)
# ----------------------------------------------------------------------------
[element.dns-entry]
kind        = leaf
# learned: 2 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# dns-forwarding  (occurs in: interface)
# ----------------------------------------------------------------------------
[element.dns-forwarding]
kind        = leaf
# learned: 4 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# dns-mgmt  (occurs in: ras-user-group)
# ----------------------------------------------------------------------------
[element.dns-mgmt]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# dns-server-list  (occurs in: system-parameters, dvcp-client)
# ----------------------------------------------------------------------------
[element.dns-server-list]
kind        = container
# learned: 2 instance(s) in reference
item_element = dns-entry
allow_empty  = true

# ----------------------------------------------------------------------------
# dnswatch  (occurs in: interface)
# ----------------------------------------------------------------------------
[element.dnswatch]
kind        = leaf
# learned: 4 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# domain  (occurs in: exception-ip)
# ----------------------------------------------------------------------------
[element.domain]
kind        = leaf
# learned: 21 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# domain-name  (occurs in: https, dvcp-client, device-conf)
# ----------------------------------------------------------------------------
[element.domain-name]
kind        = container
# learned: 7 instance(s) in reference
item_element = rules
allow_empty  = true

# ----------------------------------------------------------------------------
# domain-name-list  (occurs in: gen-cert-id)
# ----------------------------------------------------------------------------
[element.domain-name-list]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# domains  (occurs in: cookies)
# ----------------------------------------------------------------------------
[element.domains]
kind        = container
# learned: 7 instance(s) in reference
item_element = rules
allow_empty  = false

# ----------------------------------------------------------------------------
# dos-enable  (occurs in: dos-item)
# ----------------------------------------------------------------------------
[element.dos-enable]
kind        = leaf
# learned: 13 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# dos-item  (occurs in: dos-prevention)
# ----------------------------------------------------------------------------
[element.dos-item]
kind        = record
# learned: 13 instance(s) in reference
required_fields = dos-enable, dos-threshold, dos-type
allowed_fields  = dos-enable, dos-threshold, dos-type

# ----------------------------------------------------------------------------
# dos-prevention  (occurs in: physical-if, system-parameters)
# ----------------------------------------------------------------------------
[element.dos-prevention]
kind        = ambiguous
# learned: 6 instance(s) in reference

# ----------------------------------------------------------------------------
# dos-threshold  (occurs in: dos-item)
# ----------------------------------------------------------------------------
[element.dos-threshold]
kind        = leaf
# learned: 13 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# dos-type  (occurs in: dos-item)
# ----------------------------------------------------------------------------
[element.dos-type]
kind        = leaf
# learned: 13 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# download  (occurs in: request)
# ----------------------------------------------------------------------------
[element.download]
kind        = record
# learned: 5 instance(s) in reference
required_fields = fallthrough
allowed_fields  = fallthrough, rule
repeating_fields = rule

# ----------------------------------------------------------------------------
# dpd-enabled  (occurs in: ike-policy)
# ----------------------------------------------------------------------------
[element.dpd-enabled]
kind        = leaf
# learned: 3 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# dpd-max-failure  (occurs in: ike-policy)
# ----------------------------------------------------------------------------
[element.dpd-max-failure]
kind        = leaf
# learned: 3 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# dpd-worry-metric  (occurs in: ike-policy)
# ----------------------------------------------------------------------------
[element.dpd-worry-metric]
kind        = leaf
# learned: 3 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# dpi-exception  (occurs in: https)
# ----------------------------------------------------------------------------
[element.dpi-exception]
kind        = record
# learned: 5 instance(s) in reference
required_fields = override
allowed_fields  = enabled, override

# ----------------------------------------------------------------------------
# drop  (occurs in: virus-found, action, error)
# ----------------------------------------------------------------------------
[element.drop]
kind        = leaf
# learned: 28 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# dsa-cert  (occurs in: local-cert)
# ----------------------------------------------------------------------------
[element.dsa-cert]
kind        = leaf
# learned: 2 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# dsa-id-type  (occurs in: local-cert)
# ----------------------------------------------------------------------------
[element.dsa-id-type]
kind        = leaf
# learned: 2 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# dvcp-client  (occurs in: system-parameters)
# ----------------------------------------------------------------------------
[element.dvcp-client]
kind        = record
# learned: 1 instance(s) in reference
allowed_fields  = backup-server-list, ca-cert, client-id, debug, dns-server-list, domain-name, enabled, lease-interval, managed, primary-server, server-port, use-template, wins-server-list

# ----------------------------------------------------------------------------
# dynamic-routing  (occurs in: system-parameters)
# ----------------------------------------------------------------------------
[element.dynamic-routing]
kind        = record
# learned: 1 instance(s) in reference
allowed_fields  = dynamic-routing-enabled, routing-protocol-list

# ----------------------------------------------------------------------------
# dynamic-routing-enabled  (occurs in: dynamic-routing)
# ----------------------------------------------------------------------------
[element.dynamic-routing-enabled]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# eap-method  (occurs in: ras-user-group)
# ----------------------------------------------------------------------------
[element.eap-method]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# ec-cert  (occurs in: local-cert)
# ----------------------------------------------------------------------------
[element.ec-cert]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# ec-id-type  (occurs in: local-cert)
# ----------------------------------------------------------------------------
[element.ec-id-type]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# ecdhe  (occurs in: client, server)
# ----------------------------------------------------------------------------
[element.ecdhe]
kind        = leaf
# learned: 26 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# eightbitmime  (occurs in: esmtp-options)
# ----------------------------------------------------------------------------
[element.eightbitmime]
kind        = leaf
# learned: 4 instance(s) in reference
value_type      = string
allowed_values  = true
allow_empty     = false

# ----------------------------------------------------------------------------
# email  (occurs in: device-conf)
# ----------------------------------------------------------------------------
[element.email]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# email-addr  (occurs in: alarm-action)
# ----------------------------------------------------------------------------
[element.email-addr]
kind        = leaf
# learned: 49 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# email-enable  (occurs in: alarm-action)
# ----------------------------------------------------------------------------
[element.email-enable]
kind        = leaf
# learned: 49 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# email-from  (occurs in: alarm-action)
# ----------------------------------------------------------------------------
[element.email-from]
kind        = leaf
# learned: 49 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# email-subject  (occurs in: alarm-action)
# ----------------------------------------------------------------------------
[element.email-subject]
kind        = leaf
# learned: 49 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# enable  (occurs in: alarm-action, policy, log)
# ----------------------------------------------------------------------------
[element.enable]
kind        = leaf
# learned: 87 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# enable-dirharvesting  (occurs in: sip, h323)
# ----------------------------------------------------------------------------
[element.enable-dirharvesting]
kind        = leaf
# learned: 2 instance(s) in reference
value_type      = string
allowed_values  = true
allow_empty     = false

# ----------------------------------------------------------------------------
# enable-global-passphrase  (occurs in: global-settings)
# ----------------------------------------------------------------------------
[element.enable-global-passphrase]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# enable-normalization  (occurs in: sip, h323)
# ----------------------------------------------------------------------------
[element.enable-normalization]
kind        = leaf
# learned: 2 instance(s) in reference
value_type      = string
allowed_values  = true
allow_empty     = false

# ----------------------------------------------------------------------------
# enable-syslog-server  (occurs in: global-settings)
# ----------------------------------------------------------------------------
[element.enable-syslog-server]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# enable-topology-hiding  (occurs in: sip, h323)
# ----------------------------------------------------------------------------
[element.enable-topology-hiding]
kind        = leaf
# learned: 2 instance(s) in reference
value_type      = string
allowed_values  = true
allow_empty     = false

# ----------------------------------------------------------------------------
# enabled  (occurs in: rule, ips, anti-virus)
# ----------------------------------------------------------------------------
[element.enabled]
kind        = leaf
# learned: 350 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# encryp-algm  (occurs in: member)
# ----------------------------------------------------------------------------
[element.encryp-algm]
kind        = leaf
# learned: 18 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# encryp-key-len  (occurs in: member)
# ----------------------------------------------------------------------------
[element.encryp-key-len]
kind        = leaf
# learned: 5 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# encryp-key-length  (occurs in: member)
# ----------------------------------------------------------------------------
[element.encryp-key-length]
kind        = leaf
# learned: 12 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# encrypted  (occurs in: anti-virus)
# ----------------------------------------------------------------------------
[element.encrypted]
kind        = record
# learned: 2 instance(s) in reference
allowed_fields  = alarm, allow, log

# ----------------------------------------------------------------------------
# encryption  (occurs in: smtp, common-logging)
# ----------------------------------------------------------------------------
[element.encryption]
kind        = ambiguous
# learned: 5 instance(s) in reference

# ----------------------------------------------------------------------------
# end-server-port  (occurs in: member)
# ----------------------------------------------------------------------------
[element.end-server-port]
kind        = leaf
# learned: 4 instance(s) in reference
value_type      = int
allow_empty     = false

# ----------------------------------------------------------------------------
# envelope  (occurs in: smtp)
# ----------------------------------------------------------------------------
[element.envelope]
kind        = record
# learned: 4 instance(s) in reference
allowed_fields  = addresses, esmtp-options, greeting, pad

# ----------------------------------------------------------------------------
# error  (occurs in: ips, anti-virus)
# ----------------------------------------------------------------------------
[element.error]
kind        = record
# learned: 46 instance(s) in reference
required_fields = log
allowed_fields  = alarm, allow, drop, lock, log, strip

# ----------------------------------------------------------------------------
# error-action  (occurs in: dlp-sensor)
# ----------------------------------------------------------------------------
[element.error-action]
kind        = record
# learned: 2 instance(s) in reference
allowed_fields  = action, alarm, log, smtp-action

# ----------------------------------------------------------------------------
# errors  (occurs in: request)
# ----------------------------------------------------------------------------
[element.errors]
kind        = record
# learned: 2 instance(s) in reference
allowed_fields  = deny, log

# ----------------------------------------------------------------------------
# esmtp-options  (occurs in: envelope)
# ----------------------------------------------------------------------------
[element.esmtp-options]
kind        = record
# learned: 4 instance(s) in reference
allowed_fields  = auth, binarymime, chunking, eightbitmime, enabled, etrn, unknown-options

# ----------------------------------------------------------------------------
# esp-transform  (occurs in: ipsec-proposal)
# ----------------------------------------------------------------------------
[element.esp-transform]
kind        = container
# learned: 12 instance(s) in reference
item_element = member
allow_empty  = false

# ----------------------------------------------------------------------------
# etrn  (occurs in: esmtp-options)
# ----------------------------------------------------------------------------
[element.etrn]
kind        = leaf
# learned: 4 instance(s) in reference
value_type      = string
allowed_values  = true
allow_empty     = false

# ----------------------------------------------------------------------------
# event-log-level  (occurs in: log-conf)
# ----------------------------------------------------------------------------
[element.event-log-level]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# event-type  (occurs in: alarm-action)
# ----------------------------------------------------------------------------
[element.event-type]
kind        = leaf
# learned: 44 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# exception-ip  (occurs in: blocked-ip-exception-list)
# ----------------------------------------------------------------------------
[element.exception-ip]
kind        = record
# learned: 32 instance(s) in reference
required_fields = description, ip-type, reason
allowed_fields  = description, domain, host-ip-addr, ip-type, reason

# ----------------------------------------------------------------------------
# exceptions  (occurs in: spam, webblocker, ips)
# ----------------------------------------------------------------------------
[element.exceptions]
kind        = record
# learned: 13 instance(s) in reference
allowed_fields  = fallthrough, match-length, rule
repeating_fields = rule

# ----------------------------------------------------------------------------
# exclude-ip-list  (occurs in: single-sign-on)
# ----------------------------------------------------------------------------
[element.exclude-ip-list]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# expires  (occurs in: logins)
# ----------------------------------------------------------------------------
[element.expires]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# expiry  (occurs in: ras-user-group)
# ----------------------------------------------------------------------------
[element.expiry]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# explicit  (occurs in: http)
# ----------------------------------------------------------------------------
[element.explicit]
kind        = record
# learned: 1 instance(s) in reference
allowed_fields  = authentication, connect, enabled, ftp, httpRules

# ----------------------------------------------------------------------------
# ext-addr-name  (occurs in: member)
# ----------------------------------------------------------------------------
[element.ext-addr-name]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# external-if  (occurs in: physical-if)
# ----------------------------------------------------------------------------
[element.external-if]
kind        = record
# learned: 1 instance(s) in reference
allowed_fields  = ddns, dhcp-client, external-type

# ----------------------------------------------------------------------------
# external-type  (occurs in: external-if)
# ----------------------------------------------------------------------------
[element.external-type]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# failback-grace-period  (occurs in: multi-wan)
# ----------------------------------------------------------------------------
[element.failback-grace-period]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# fallthrough  (occurs in: rules, headers, content-types)
# ----------------------------------------------------------------------------
[element.fallthrough]
kind        = ambiguous
# learned: 165 instance(s) in reference

# ----------------------------------------------------------------------------
# feature-details-list  (occurs in: ui-pm)
# ----------------------------------------------------------------------------
[element.feature-details-list]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# feature-key-auto-sync  (occurs in: lsd)
# ----------------------------------------------------------------------------
[element.feature-key-auto-sync]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# feature-key-url  (occurs in: lsd)
# ----------------------------------------------------------------------------
[element.feature-key-url]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# file-patterns  (occurs in: content-checking)
# ----------------------------------------------------------------------------
[element.file-patterns]
kind        = record
# learned: 10 instance(s) in reference
required_fields = fallthrough
allowed_fields  = fallthrough, rule
repeating_fields = rule

# ----------------------------------------------------------------------------
# filename  (occurs in: limits)
# ----------------------------------------------------------------------------
[element.filename]
kind        = record
# learned: 5 instance(s) in reference
required_fields = action, value
allowed_fields  = action, value

# ----------------------------------------------------------------------------
# filter  (occurs in: uri, https, filter-list)
# ----------------------------------------------------------------------------
[element.filter]
kind        = record
# learned: 14 instance(s) in reference
allowed_fields  = bypass, destination, dlp-action, enabled, helper-name, source

# ----------------------------------------------------------------------------
# filter-list  (occurs in: dlp-sensor)
# ----------------------------------------------------------------------------
[element.filter-list]
kind        = container
# learned: 2 instance(s) in reference
item_element = filter
allow_empty  = false

# ----------------------------------------------------------------------------
# firebox-ip  (occurs in: abs-muvpn)
# ----------------------------------------------------------------------------
[element.firebox-ip]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# firebox-user-case-sensitivity  (occurs in: auth-global-setting)
# ----------------------------------------------------------------------------
[element.firebox-user-case-sensitivity]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# firewall  (occurs in: policy, abs-policy)
# ----------------------------------------------------------------------------
[element.firewall]
kind        = leaf
# learned: 31 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# for-model  (occurs in: device-conf)
# ----------------------------------------------------------------------------
[element.for-model]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# for-version  (occurs in: profile)
# ----------------------------------------------------------------------------
[element.for-version]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# force-renew  (occurs in: dhcp-client)
# ----------------------------------------------------------------------------
[element.force-renew]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# format  (occurs in: message)
# ----------------------------------------------------------------------------
[element.format]
kind        = record
# learned: 10 instance(s) in reference
required_fields = log
allowed_fields  = deny, log, strip

# ----------------------------------------------------------------------------
# forward-traffic-mgmt  (occurs in: policy, settings)
# ----------------------------------------------------------------------------
[element.forward-traffic-mgmt]
kind        = leaf
# learned: 16 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# from  (occurs in: addresses)
# ----------------------------------------------------------------------------
[element.from]
kind        = record
# learned: 4 instance(s) in reference
allowed_fields  = fallthrough, rule
repeating_fields = rule

# ----------------------------------------------------------------------------
# from-alias  (occurs in: dnat)
# ----------------------------------------------------------------------------
[element.from-alias]
kind        = leaf
# learned: 3 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# from-alias-list  (occurs in: policy, abs-policy)
# ----------------------------------------------------------------------------
[element.from-alias-list]
kind        = container
# learned: 31 instance(s) in reference
item_element = alias
allow_empty  = false

# ----------------------------------------------------------------------------
# from-hour  (occurs in: member)
# ----------------------------------------------------------------------------
[element.from-hour]
kind        = leaf
# learned: 6 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# from-min  (occurs in: member)
# ----------------------------------------------------------------------------
[element.from-min]
kind        = leaf
# learned: 6 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# ftp  (occurs in: proxy-action, explicit)
# ----------------------------------------------------------------------------
[element.ftp]
kind        = record
# learned: 6 instance(s) in reference
allowed_fields  = anti-virus, apt-enabled, dlp-sensor-list, enabled, ips, password, redirect-to, request, transaction, username

# ----------------------------------------------------------------------------
# full-duplex  (occurs in: physical-if)
# ----------------------------------------------------------------------------
[element.full-duplex]
kind        = leaf
# learned: 5 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# full-scan-mode  (occurs in: ips)
# ----------------------------------------------------------------------------
[element.full-scan-mode]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# gateway-ip  (occurs in: route-entry)
# ----------------------------------------------------------------------------
[element.gateway-ip]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# gateway-wireless-controller  (occurs in: profile)
# ----------------------------------------------------------------------------
[element.gateway-wireless-controller]
kind        = record
# learned: 1 instance(s) in reference
allowed_fields  = ap-list, global-settings, policy-domain-list, ssid-list

# ----------------------------------------------------------------------------
# gen-cert-id  (occurs in: local-cert)
# ----------------------------------------------------------------------------
[element.gen-cert-id]
kind        = record
# learned: 1 instance(s) in reference
allowed_fields  = domain-name-list, ip-list

# ----------------------------------------------------------------------------
# geo-action  (occurs in: policy, settings, geo-action-list)
# ----------------------------------------------------------------------------
[element.geo-action]
kind        = ambiguous
# learned: 17 instance(s) in reference

# ----------------------------------------------------------------------------
# geo-action-list  (occurs in: geolocation-blocking)
# ----------------------------------------------------------------------------
[element.geo-action-list]
kind        = container
# learned: 1 instance(s) in reference
item_element = geo-action
allow_empty  = false

# ----------------------------------------------------------------------------
# geo-list  (occurs in: geo-action)
# ----------------------------------------------------------------------------
[element.geo-list]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# geolocation-blocking  (occurs in: profile)
# ----------------------------------------------------------------------------
[element.geolocation-blocking]
kind        = record
# learned: 1 instance(s) in reference
allowed_fields  = enabled, geo-action-list

# ----------------------------------------------------------------------------
# global-1to1-nat  (occurs in: policy)
# ----------------------------------------------------------------------------
[element.global-1to1-nat]
kind        = leaf
# learned: 16 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# global-dnat  (occurs in: policy)
# ----------------------------------------------------------------------------
[element.global-dnat]
kind        = leaf
# learned: 16 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# global-idle-timeout  (occurs in: ras-user-group)
# ----------------------------------------------------------------------------
[element.global-idle-timeout]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# global-settings  (occurs in: ui-pm, gateway-wireless-controller)
# ----------------------------------------------------------------------------
[element.global-settings]
kind        = record
# learned: 2 instance(s) in reference
allowed_fields  = auto-firmware-update, enable-global-passphrase, enable-syslog-server, gwc-enabled

# ----------------------------------------------------------------------------
# greeting  (occurs in: envelope)
# ----------------------------------------------------------------------------
[element.greeting]
kind        = record
# learned: 4 instance(s) in reference
allowed_fields  = fallthrough, rule
repeating_fields = rule

# ----------------------------------------------------------------------------
# gwc-enabled  (occurs in: global-settings)
# ----------------------------------------------------------------------------
[element.gwc-enabled]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# h323  (occurs in: proxy-action)
# ----------------------------------------------------------------------------
[element.h323]
kind        = record
# learned: 1 instance(s) in reference
allowed_fields  = acl, denied-codecs, enable-dirharvesting, enable-normalization, enable-topology-hiding, max-sessions, timeout, transaction, user-agent

# ----------------------------------------------------------------------------
# header  (occurs in: request, response)
# ----------------------------------------------------------------------------
[element.header]
kind        = record
# learned: 14 instance(s) in reference
required_fields = max-line-length, max-total-length
allowed_fields  = anonymize, authorization, content-type, cookies, known, max-line-length, max-total-length, transfer-encoding

# ----------------------------------------------------------------------------
# headers  (occurs in: message)
# ----------------------------------------------------------------------------
[element.headers]
kind        = record
# learned: 10 instance(s) in reference
required_fields = fallthrough, rule
allowed_fields  = fallthrough, rule
repeating_fields = rule

# ----------------------------------------------------------------------------
# helo-name  (occurs in: masquerading)
# ----------------------------------------------------------------------------
[element.helo-name]
kind        = leaf
# learned: 4 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# helper  (occurs in: spam)
# ----------------------------------------------------------------------------
[element.helper]
kind        = leaf
# learned: 10 instance(s) in reference
value_type      = string
allowed_values  = ctspam
allow_empty     = false

# ----------------------------------------------------------------------------
# helper-name  (occurs in: filter)
# ----------------------------------------------------------------------------
[element.helper-name]
kind        = leaf
# learned: 12 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# high  (occurs in: threat-level, apt)
# ----------------------------------------------------------------------------
[element.high]
kind        = record
# learned: 2 instance(s) in reference
allowed_fields  = action, alarm, log

# ----------------------------------------------------------------------------
# host  (occurs in: log-server)
# ----------------------------------------------------------------------------
[element.host]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# host-ip-addr  (occurs in: exception-ip, member)
# ----------------------------------------------------------------------------
[element.host-ip-addr]
kind        = leaf
# learned: 18 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# host-unreachable  (occurs in: icmp-error-handling)
# ----------------------------------------------------------------------------
[element.host-unreachable]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# hotspot-list  (occurs in: profile)
# ----------------------------------------------------------------------------
[element.hotspot-list]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# http  (occurs in: proxy-action)
# ----------------------------------------------------------------------------
[element.http]
kind        = record
# learned: 7 instance(s) in reference
required_fields = anti-virus, apps-block-profile, deny-message, ips, request, response
allowed_fields  = anti-virus, apps-block-profile, apt-enabled, content-rules, deny-message, dlp-sensor-list, explicit, ips, redirect, reputation-authority, request, response, trusted-hosts, warn-message

# ----------------------------------------------------------------------------
# httpRules  (occurs in: explicit)
# ----------------------------------------------------------------------------
[element.httpRules]
kind        = container
# learned: 1 instance(s) in reference
item_element = via
allow_empty  = false

# ----------------------------------------------------------------------------
# https  (occurs in: proxy-action)
# ----------------------------------------------------------------------------
[element.https]
kind        = record
# learned: 5 instance(s) in reference
required_fields = domain-name, dpi-exception, filter, redirect-to, sslfilter, timeout, tls-profile, transaction
allowed_fields  = allow-non-ssl, domain-name, dpi-exception, filter, redirect-to, sslfilter, timeout, tls-profile, transaction

# ----------------------------------------------------------------------------
# https-exception-override  (occurs in: https-exception-override-list)
# ----------------------------------------------------------------------------
[element.https-exception-override]
kind        = record
# learned: 1 instance(s) in reference
allowed_fields  = name, override-id-list

# ----------------------------------------------------------------------------
# https-exception-override-list  (occurs in: profile)
# ----------------------------------------------------------------------------
[element.https-exception-override-list]
kind        = container
# learned: 1 instance(s) in reference
item_element = https-exception-override
allow_empty  = false

# ----------------------------------------------------------------------------
# icmp-code  (occurs in: member)
# ----------------------------------------------------------------------------
[element.icmp-code]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = int
allow_empty     = false

# ----------------------------------------------------------------------------
# icmp-error-handling  (occurs in: system-parameters)
# ----------------------------------------------------------------------------
[element.icmp-error-handling]
kind        = record
# learned: 1 instance(s) in reference
allowed_fields  = all-icmp-errors, host-unreachable, network-unreachable, pmtu-discovery, port-unreachable, protocol-unreachable, time-exceeded

# ----------------------------------------------------------------------------
# icmp-timeout  (occurs in: traffic-flow-timers)
# ----------------------------------------------------------------------------
[element.icmp-timeout]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# icmp-type  (occurs in: member)
# ----------------------------------------------------------------------------
[element.icmp-type]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = int
allow_empty     = false

# ----------------------------------------------------------------------------
# idle-timeout  (occurs in: service, policy, settings)
# ----------------------------------------------------------------------------
[element.idle-timeout]
kind        = leaf
# learned: 138 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# if-dev-name  (occurs in: physical-if)
# ----------------------------------------------------------------------------
[element.if-dev-name]
kind        = leaf
# learned: 5 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# if-item-list  (occurs in: interface)
# ----------------------------------------------------------------------------
[element.if-item-list]
kind        = container
# learned: 7 instance(s) in reference
item_element = item
allow_empty  = false

# ----------------------------------------------------------------------------
# if-num  (occurs in: physical-if)
# ----------------------------------------------------------------------------
[element.if-num]
kind        = leaf
# learned: 5 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# if-property  (occurs in: physical-if)
# ----------------------------------------------------------------------------
[element.if-property]
kind        = leaf
# learned: 5 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# ike-action  (occurs in: ike-policy, ike-action-list)
# ----------------------------------------------------------------------------
[element.ike-action]
kind        = ambiguous
# learned: 5 instance(s) in reference

# ----------------------------------------------------------------------------
# ike-action-list  (occurs in: profile)
# ----------------------------------------------------------------------------
[element.ike-action-list]
kind        = container
# learned: 1 instance(s) in reference
item_element = ike-action
allow_empty  = false

# ----------------------------------------------------------------------------
# ike-decryp-pkt-trace  (occurs in: log-conf)
# ----------------------------------------------------------------------------
[element.ike-decryp-pkt-trace]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# ike-policy  (occurs in: ike-policy-list, ipsec-action, member)
# ----------------------------------------------------------------------------
[element.ike-policy]
kind        = ambiguous
# learned: 6 instance(s) in reference

# ----------------------------------------------------------------------------
# ike-policy-group  (occurs in: ipsec-action, ike-policy-group-list)
# ----------------------------------------------------------------------------
[element.ike-policy-group]
kind        = ambiguous
# learned: 2 instance(s) in reference

# ----------------------------------------------------------------------------
# ike-policy-group-list  (occurs in: profile)
# ----------------------------------------------------------------------------
[element.ike-policy-group-list]
kind        = container
# learned: 1 instance(s) in reference
item_element = ike-policy-group
allow_empty  = false

# ----------------------------------------------------------------------------
# ike-policy-list  (occurs in: profile)
# ----------------------------------------------------------------------------
[element.ike-policy-list]
kind        = container
# learned: 1 instance(s) in reference
item_element = ike-policy
allow_empty  = false

# ----------------------------------------------------------------------------
# ike-transform  (occurs in: ike-action)
# ----------------------------------------------------------------------------
[element.ike-transform]
kind        = container
# learned: 2 instance(s) in reference
item_element = member
allow_empty  = false

# ----------------------------------------------------------------------------
# imap  (occurs in: proxy-action)
# ----------------------------------------------------------------------------
[element.imap]
kind        = record
# learned: 2 instance(s) in reference
allowed_fields  = anti-virus, message, protocol, transaction

# ----------------------------------------------------------------------------
# imaps  (occurs in: message)
# ----------------------------------------------------------------------------
[element.imaps]
kind        = record
# learned: 2 instance(s) in reference
allowed_fields  = action, tls-profile

# ----------------------------------------------------------------------------
# info  (occurs in: threat-level)
# ----------------------------------------------------------------------------
[element.info]
kind        = record
# learned: 1 instance(s) in reference
allowed_fields  = action, alarm, log

# ----------------------------------------------------------------------------
# instance-name  (occurs in: condition)
# ----------------------------------------------------------------------------
[element.instance-name]
kind        = leaf
# learned: 14 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# interface  (occurs in: alias-member, interface-list, member)
# ----------------------------------------------------------------------------
[element.interface]
kind        = ambiguous
# learned: 37 instance(s) in reference

# ----------------------------------------------------------------------------
# interface-list  (occurs in: profile)
# ----------------------------------------------------------------------------
[element.interface-list]
kind        = container
# learned: 1 instance(s) in reference
item_element = interface
allow_empty  = false

# ----------------------------------------------------------------------------
# interval  (occurs in: alarm-action)
# ----------------------------------------------------------------------------
[element.interval]
kind        = leaf
# learned: 2 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# intra-inspection  (occurs in: physical-if)
# ----------------------------------------------------------------------------
[element.intra-inspection]
kind        = leaf
# learned: 5 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# ip  (occurs in: physical-if, radius-conf, primary-server)
# ----------------------------------------------------------------------------
[element.ip]
kind        = leaf
# learned: 9 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# ip-addr  (occurs in: peer-auth)
# ----------------------------------------------------------------------------
[element.ip-addr]
kind        = leaf
# learned: 3 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# ip-list  (occurs in: gen-cert-id)
# ----------------------------------------------------------------------------
[element.ip-list]
kind        = container
# learned: 1 instance(s) in reference
item_element = ip
allow_empty  = false

# ----------------------------------------------------------------------------
# ip-mask  (occurs in: member)
# ----------------------------------------------------------------------------
[element.ip-mask]
kind        = leaf
# learned: 8 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# ip-network-addr  (occurs in: member, resource-addr)
# ----------------------------------------------------------------------------
[element.ip-network-addr]
kind        = leaf
# learned: 9 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# ip-type  (occurs in: exception-ip)
# ----------------------------------------------------------------------------
[element.ip-type]
kind        = leaf
# learned: 32 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# ips  (occurs in: http, ftp, smtp)
# ----------------------------------------------------------------------------
[element.ips]
kind        = ambiguous
# learned: 26 instance(s) in reference

# ----------------------------------------------------------------------------
# ips-monitor-enabled  (occurs in: policy, settings)
# ----------------------------------------------------------------------------
[element.ips-monitor-enabled]
kind        = leaf
# learned: 31 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# ips-signature-exception-list  (occurs in: sigeng)
# ----------------------------------------------------------------------------
[element.ips-signature-exception-list]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# ipsec-action  (occurs in: ipsec-action-list, item, abs-muvpn)
# ----------------------------------------------------------------------------
[element.ipsec-action]
kind        = ambiguous
# learned: 6 instance(s) in reference

# ----------------------------------------------------------------------------
# ipsec-action-list  (occurs in: profile)
# ----------------------------------------------------------------------------
[element.ipsec-action-list]
kind        = container
# learned: 1 instance(s) in reference
item_element = ipsec-action
allow_empty  = false

# ----------------------------------------------------------------------------
# ipsec-pass-through  (occurs in: vpn-global-setting)
# ----------------------------------------------------------------------------
[element.ipsec-pass-through]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# ipsec-proposal  (occurs in: ipsec-proposal-list, ipsec-action)
# ----------------------------------------------------------------------------
[element.ipsec-proposal]
kind        = record
# learned: 14 instance(s) in reference
allowed_fields  = anti-replay-window, description, esp-transform, member, name, property, type
repeating_fields = member

# ----------------------------------------------------------------------------
# ipsec-proposal-list  (occurs in: profile)
# ----------------------------------------------------------------------------
[element.ipsec-proposal-list]
kind        = container
# learned: 1 instance(s) in reference
item_element = ipsec-proposal
allow_empty  = false

# ----------------------------------------------------------------------------
# item  (occurs in: if-item-list, auth-group-item)
# ----------------------------------------------------------------------------
[element.item]
kind        = record
# learned: 8 instance(s) in reference
allowed_fields  = auth-domain, ipsec-action, item-type, membership-id, physical-if, type

# ----------------------------------------------------------------------------
# item-type  (occurs in: item)
# ----------------------------------------------------------------------------
[element.item-type]
kind        = leaf
# learned: 7 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# keep-alive-interval  (occurs in: ike-policy)
# ----------------------------------------------------------------------------
[element.keep-alive-interval]
kind        = leaf
# learned: 3 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# keep-alive-max  (occurs in: ike-policy)
# ----------------------------------------------------------------------------
[element.keep-alive-max]
kind        = leaf
# learned: 3 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# key-mode  (occurs in: ipsec-action)
# ----------------------------------------------------------------------------
[element.key-mode]
kind        = leaf
# learned: 2 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# known  (occurs in: header)
# ----------------------------------------------------------------------------
[element.known]
kind        = container
# learned: 7 instance(s) in reference
item_element = rules
allow_empty  = false

# ----------------------------------------------------------------------------
# l2tp-list  (occurs in: profile)
# ----------------------------------------------------------------------------
[element.l2tp-list]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# launch-interval  (occurs in: alarm-action, settings)
# ----------------------------------------------------------------------------
[element.launch-interval]
kind        = leaf
# learned: 63 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# ldap  (occurs in: system-parameters)
# ----------------------------------------------------------------------------
[element.ldap]
kind        = record
# learned: 1 instance(s) in reference
allowed_fields  = ldap-server-ip, ldap-server-name, ldap-server-password, ldap-server-port, use-ip, use-ldap

# ----------------------------------------------------------------------------
# ldap-server-ip  (occurs in: ldap)
# ----------------------------------------------------------------------------
[element.ldap-server-ip]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# ldap-server-name  (occurs in: ldap)
# ----------------------------------------------------------------------------
[element.ldap-server-name]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# ldap-server-password  (occurs in: ldap)
# ----------------------------------------------------------------------------
[element.ldap-server-password]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# ldap-server-port  (occurs in: ldap)
# ----------------------------------------------------------------------------
[element.ldap-server-port]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = int
allow_empty     = false

# ----------------------------------------------------------------------------
# lease-interval  (occurs in: dvcp-client)
# ----------------------------------------------------------------------------
[element.lease-interval]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# lease-time  (occurs in: dhcp-client)
# ----------------------------------------------------------------------------
[element.lease-time]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# license-bypass  (occurs in: webblocker)
# ----------------------------------------------------------------------------
[element.license-bypass]
kind        = leaf
# learned: 2 instance(s) in reference
value_type      = string
allowed_values  = false
allow_empty     = false

# ----------------------------------------------------------------------------
# life-length  (occurs in: member)
# ----------------------------------------------------------------------------
[element.life-length]
kind        = leaf
# learned: 18 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# lifetime  (occurs in: member)
# ----------------------------------------------------------------------------
[element.lifetime]
kind        = leaf
# learned: 18 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# limits  (occurs in: message, request)
# ----------------------------------------------------------------------------
[element.limits]
kind        = record
# learned: 15 instance(s) in reference
allowed_fields  = deny, filename, line, log, logins, password, username

# ----------------------------------------------------------------------------
# line  (occurs in: limits)
# ----------------------------------------------------------------------------
[element.line]
kind        = record
# learned: 5 instance(s) in reference
required_fields = action, value
allowed_fields  = action, value

# ----------------------------------------------------------------------------
# line-conn-mode  (occurs in: abs-muvpn)
# ----------------------------------------------------------------------------
[element.line-conn-mode]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# line-inactivity-timeout  (occurs in: abs-muvpn)
# ----------------------------------------------------------------------------
[element.line-inactivity-timeout]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# link-speed  (occurs in: physical-if)
# ----------------------------------------------------------------------------
[element.link-speed]
kind        = leaf
# learned: 5 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# load-balance-list  (occurs in: profile)
# ----------------------------------------------------------------------------
[element.load-balance-list]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# local-addr  (occurs in: local-remote-pair)
# ----------------------------------------------------------------------------
[element.local-addr]
kind        = ambiguous
# learned: 3 instance(s) in reference

# ----------------------------------------------------------------------------
# local-cert  (occurs in: ike-policy)
# ----------------------------------------------------------------------------
[element.local-cert]
kind        = record
# learned: 3 instance(s) in reference
allowed_fields  = dsa-cert, dsa-id-type, ec-cert, ec-id-type, gen-cert-id, local-id-data, local-id-type, local-if, psk, psk-hex, rsa-cert, rsa-id, rsa-id-type

# ----------------------------------------------------------------------------
# local-enabled  (occurs in: log-conf)
# ----------------------------------------------------------------------------
[element.local-enabled]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# local-id-data  (occurs in: local-cert)
# ----------------------------------------------------------------------------
[element.local-id-data]
kind        = leaf
# learned: 3 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# local-id-type  (occurs in: local-cert)
# ----------------------------------------------------------------------------
[element.local-id-type]
kind        = leaf
# learned: 3 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# local-if  (occurs in: local-cert)
# ----------------------------------------------------------------------------
[element.local-if]
kind        = leaf
# learned: 3 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# local-remote-pair  (occurs in: local-remote-pair-list)
# ----------------------------------------------------------------------------
[element.local-remote-pair]
kind        = record
# learned: 3 instance(s) in reference
allowed_fields  = direction, dnat-enabled, dnat-src-ip, local-addr, nat-action, one-to-one-nat-enabled, remote-addr

# ----------------------------------------------------------------------------
# local-remote-pair-list  (occurs in: ipsec-action, abs-ipsec-action)
# ----------------------------------------------------------------------------
[element.local-remote-pair-list]
kind        = container
# learned: 3 instance(s) in reference
item_element = local-remote-pair
allow_empty  = false

# ----------------------------------------------------------------------------
# location  (occurs in: device-conf)
# ----------------------------------------------------------------------------
[element.location]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# lock  (occurs in: error)
# ----------------------------------------------------------------------------
[element.lock]
kind        = leaf
# learned: 4 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# log  (occurs in: rule, fallthrough, action)
# ----------------------------------------------------------------------------
[element.log]
kind        = ambiguous
# learned: 1983 instance(s) in reference

# ----------------------------------------------------------------------------
# log-clean  (occurs in: apt)
# ----------------------------------------------------------------------------
[element.log-clean]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# log-conf  (occurs in: system-parameters)
# ----------------------------------------------------------------------------
[element.log-conf]
kind        = record
# learned: 1 instance(s) in reference
allowed_fields  = debug-level, event-log-level, ike-decryp-pkt-trace, local-enabled, support-cache-enabled, traffic-log-enable

# ----------------------------------------------------------------------------
# log-dlp-stats  (occurs in: dlp)
# ----------------------------------------------------------------------------
[element.log-dlp-stats]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# log-dlp-stats-interval  (occurs in: dlp)
# ----------------------------------------------------------------------------
[element.log-dlp-stats-interval]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# log-enabled  (occurs in: settings)
# ----------------------------------------------------------------------------
[element.log-enabled]
kind        = leaf
# learned: 15 instance(s) in reference
value_type      = string
allowed_values  = false, true
allow_empty     = false

# ----------------------------------------------------------------------------
# log-exception  (occurs in: spam)
# ----------------------------------------------------------------------------
[element.log-exception]
kind        = record
# learned: 10 instance(s) in reference
required_fields = allow, log
allowed_fields  = allow, log

# ----------------------------------------------------------------------------
# log-for-report  (occurs in: policy, settings)
# ----------------------------------------------------------------------------
[element.log-for-report]
kind        = leaf
# learned: 16 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# log-in-out-broadcast  (occurs in: misc-global-setting)
# ----------------------------------------------------------------------------
[element.log-in-out-broadcast]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# log-server  (occurs in: common-logging)
# ----------------------------------------------------------------------------
[element.log-server]
kind        = record
# learned: 1 instance(s) in reference
allowed_fields  = host, password, port

# ----------------------------------------------------------------------------
# logins  (occurs in: limits)
# ----------------------------------------------------------------------------
[element.logins]
kind        = record
# learned: 5 instance(s) in reference
required_fields = action, value
allowed_fields  = action, expires, value

# ----------------------------------------------------------------------------
# low  (occurs in: threat-level, apt)
# ----------------------------------------------------------------------------
[element.low]
kind        = record
# learned: 2 instance(s) in reference
allowed_fields  = action, alarm, log

# ----------------------------------------------------------------------------
# lsd  (occurs in: system-parameters)
# ----------------------------------------------------------------------------
[element.lsd]
kind        = record
# learned: 1 instance(s) in reference
allowed_fields  = debug, enable, feature-key-auto-sync, feature-key-url, rss-check, rss-url, serial, staging-url

# ----------------------------------------------------------------------------
# mac-address  (occurs in: physical-if)
# ----------------------------------------------------------------------------
[element.mac-address]
kind        = leaf
# learned: 5 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# mac-address-enable  (occurs in: physical-if)
# ----------------------------------------------------------------------------
[element.mac-address-enable]
kind        = leaf
# learned: 5 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# managed  (occurs in: dvcp-client)
# ----------------------------------------------------------------------------
[element.managed]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# management-server-ip  (occurs in: vpn-global-setting)
# ----------------------------------------------------------------------------
[element.management-server-ip]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# marking-field  (occurs in: qos-marking)
# ----------------------------------------------------------------------------
[element.marking-field]
kind        = leaf
# learned: 36 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# marking-method  (occurs in: qos-marking)
# ----------------------------------------------------------------------------
[element.marking-method]
kind        = container
# learned: 36 instance(s) in reference
item_element = marking-type
allow_empty  = false

# ----------------------------------------------------------------------------
# marking-type  (occurs in: marking-method)
# ----------------------------------------------------------------------------
[element.marking-type]
kind        = leaf
# learned: 36 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# mask  (occurs in: route-entry)
# ----------------------------------------------------------------------------
[element.mask]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# masquerade  (occurs in: protocol)
# ----------------------------------------------------------------------------
[element.masquerade]
kind        = container
# learned: 4 instance(s) in reference
item_element = server-replies
allow_empty  = false

# ----------------------------------------------------------------------------
# masquerading  (occurs in: smtp)
# ----------------------------------------------------------------------------
[element.masquerading]
kind        = record
# learned: 4 instance(s) in reference
allowed_fields  = banner-name, helo-name, message-id, server-replies

# ----------------------------------------------------------------------------
# match-length  (occurs in: rules, exceptions)
# ----------------------------------------------------------------------------
[element.match-length]
kind        = leaf
# learned: 72 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# max-extracted-text-size  (occurs in: dlp)
# ----------------------------------------------------------------------------
[element.max-extracted-text-size]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# max-length  (occurs in: uri)
# ----------------------------------------------------------------------------
[element.max-length]
kind        = ambiguous
# learned: 14 instance(s) in reference

# ----------------------------------------------------------------------------
# max-line-length  (occurs in: header)
# ----------------------------------------------------------------------------
[element.max-line-length]
kind        = record
# learned: 14 instance(s) in reference
required_fields = action, value
allowed_fields  = action, value

# ----------------------------------------------------------------------------
# max-link-bandwidth  (occurs in: qos)
# ----------------------------------------------------------------------------
[element.max-link-bandwidth]
kind        = leaf
# learned: 5 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# max-scan-file-size  (occurs in: ctspam)
# ----------------------------------------------------------------------------
[element.max-scan-file-size]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# max-sessions  (occurs in: sip, h323)
# ----------------------------------------------------------------------------
[element.max-sessions]
kind        = leaf
# learned: 2 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# max-threads  (occurs in: ctspam)
# ----------------------------------------------------------------------------
[element.max-threads]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# max-total-length  (occurs in: header)
# ----------------------------------------------------------------------------
[element.max-total-length]
kind        = record
# learned: 14 instance(s) in reference
required_fields = action, value
allowed_fields  = action, value

# ----------------------------------------------------------------------------
# maximum-length  (occurs in: addresses)
# ----------------------------------------------------------------------------
[element.maximum-length]
kind        = leaf
# learned: 4 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# maximum-line-length  (occurs in: message)
# ----------------------------------------------------------------------------
[element.maximum-line-length]
kind        = leaf
# learned: 10 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# maximum-request-length  (occurs in: protocol)
# ----------------------------------------------------------------------------
[element.maximum-request-length]
kind        = leaf
# learned: 4 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# maximum-response-length  (occurs in: protocol)
# ----------------------------------------------------------------------------
[element.maximum-response-length]
kind        = leaf
# learned: 4 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# maximum-size  (occurs in: message)
# ----------------------------------------------------------------------------
[element.maximum-size]
kind        = leaf
# learned: 4 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# maximum-to  (occurs in: addresses)
# ----------------------------------------------------------------------------
[element.maximum-to]
kind        = leaf
# learned: 4 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# medium  (occurs in: threat-level, apt)
# ----------------------------------------------------------------------------
[element.medium]
kind        = record
# learned: 2 instance(s) in reference
allowed_fields  = action, alarm, log

# ----------------------------------------------------------------------------
# member  (occurs in: service-item, addr-group-member, esp-transform)
# ----------------------------------------------------------------------------
[element.member]
kind        = ambiguous
# learned: 209 instance(s) in reference

# ----------------------------------------------------------------------------
# member-list  (occurs in: ike-policy-group)
# ----------------------------------------------------------------------------
[element.member-list]
kind        = container
# learned: 1 instance(s) in reference
item_element = member
allow_empty  = false

# ----------------------------------------------------------------------------
# membership-id  (occurs in: item)
# ----------------------------------------------------------------------------
[element.membership-id]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# message  (occurs in: smtp, pop3, imap)
# ----------------------------------------------------------------------------
[element.message]
kind        = record
# learned: 10 instance(s) in reference
required_fields = content-checking, format, headers, limits, maximum-line-length, spam
allowed_fields  = content-checking, format, headers, imaps, limits, maximum-line-length, maximum-size, pop3s, spam

# ----------------------------------------------------------------------------
# message-id  (occurs in: masquerading)
# ----------------------------------------------------------------------------
[element.message-id]
kind        = leaf
# learned: 4 instance(s) in reference
value_type      = string
allowed_values  = false
allow_empty     = false

# ----------------------------------------------------------------------------
# messages  (occurs in: content-checking)
# ----------------------------------------------------------------------------
[element.messages]
kind        = container
# learned: 4 instance(s) in reference
item_element = deny
allow_empty  = false

# ----------------------------------------------------------------------------
# method  (occurs in: request)
# ----------------------------------------------------------------------------
[element.method]
kind        = record
# learned: 7 instance(s) in reference
required_fields = rules, webdav-mech
allowed_fields  = rules, webdav-mech

# ----------------------------------------------------------------------------
# metric  (occurs in: route-entry)
# ----------------------------------------------------------------------------
[element.metric]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# mgmt-user-idle-timeout  (occurs in: auth-global-setting)
# ----------------------------------------------------------------------------
[element.mgmt-user-idle-timeout]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# mgmt-user-session-timeout  (occurs in: auth-global-setting)
# ----------------------------------------------------------------------------
[element.mgmt-user-session-timeout]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# mgr-station-list  (occurs in: snmp-conf)
# ----------------------------------------------------------------------------
[element.mgr-station-list]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# misc-global-setting  (occurs in: system-parameters)
# ----------------------------------------------------------------------------
[element.misc-global-setting]
kind        = record
# learned: 1 instance(s) in reference
allowed_fields  = auto-blocked-duration, auto-order-enabled, block-spoof-enabled, log-in-out-broadcast, qos-enable, syn-checking-enable, vlan-forward

# ----------------------------------------------------------------------------
# mode  (occurs in: ipsec-action, ike-action)
# ----------------------------------------------------------------------------
[element.mode]
kind        = leaf
# learned: 4 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# module-id  (occurs in: revision)
# ----------------------------------------------------------------------------
[element.module-id]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# monitord  (occurs in: system-parameters)
# ----------------------------------------------------------------------------
[element.monitord]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# mss-adjustment  (occurs in: system-parameters)
# ----------------------------------------------------------------------------
[element.mss-adjustment]
kind        = record
# learned: 1 instance(s) in reference
allowed_fields  = mss-adjustment-type, mss-maximum-limit

# ----------------------------------------------------------------------------
# mss-adjustment-type  (occurs in: mss-adjustment)
# ----------------------------------------------------------------------------
[element.mss-adjustment-type]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# mss-maximum-limit  (occurs in: mss-adjustment)
# ----------------------------------------------------------------------------
[element.mss-maximum-limit]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# mtu  (occurs in: physical-if)
# ----------------------------------------------------------------------------
[element.mtu]
kind        = leaf
# learned: 5 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# multi-wan  (occurs in: system-parameters)
# ----------------------------------------------------------------------------
[element.multi-wan]
kind        = record
# learned: 1 instance(s) in reference
allowed_fields  = algorithm, failback-grace-period, others-sticky-timer, tcp-sticky-timer, udp-sticky-timer

# ----------------------------------------------------------------------------
# multicast-if  (occurs in: interface)
# ----------------------------------------------------------------------------
[element.multicast-if]
kind        = record
# learned: 5 instance(s) in reference
required_fields = enabled, rp-candidate
allowed_fields  = enabled, rp-candidate

# ----------------------------------------------------------------------------
# muvpn-security-readonly  (occurs in: vpn-global-setting)
# ----------------------------------------------------------------------------
[element.muvpn-security-readonly]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# muvpn-users-list  (occurs in: abs-policy)
# ----------------------------------------------------------------------------
[element.muvpn-users-list]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# muvpn-virtual-adapter-settings  (occurs in: vpn-global-setting)
# ----------------------------------------------------------------------------
[element.muvpn-virtual-adapter-settings]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# name  (occurs in: rule, service, alarm-action)
# ----------------------------------------------------------------------------
[element.name]
kind        = leaf
# learned: 1757 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# nat  (occurs in: policy, nat-list)
# ----------------------------------------------------------------------------
[element.nat]
kind        = record
# learned: 6 instance(s) in reference
allowed_fields  = algorithm, description, name, nat-item, property, proxy-arp, type

# ----------------------------------------------------------------------------
# nat-action  (occurs in: local-remote-pair)
# ----------------------------------------------------------------------------
[element.nat-action]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# nat-item  (occurs in: nat)
# ----------------------------------------------------------------------------
[element.nat-item]
kind        = container
# learned: 1 instance(s) in reference
item_element = member
allow_empty  = false

# ----------------------------------------------------------------------------
# nat-list  (occurs in: profile)
# ----------------------------------------------------------------------------
[element.nat-list]
kind        = container
# learned: 1 instance(s) in reference
item_element = nat
allow_empty  = false

# ----------------------------------------------------------------------------
# nat-t-config  (occurs in: ike-action)
# ----------------------------------------------------------------------------
[element.nat-t-config]
kind        = record
# learned: 2 instance(s) in reference
allowed_fields  = nat-t-enabled, nat-t-from-port, nat-t-keep-alive, nat-t-to-port, nat-t-udp-checksum-enabled

# ----------------------------------------------------------------------------
# nat-t-enabled  (occurs in: nat-t-config)
# ----------------------------------------------------------------------------
[element.nat-t-enabled]
kind        = leaf
# learned: 2 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# nat-t-from-port  (occurs in: nat-t-config)
# ----------------------------------------------------------------------------
[element.nat-t-from-port]
kind        = leaf
# learned: 2 instance(s) in reference
value_type      = int
allow_empty     = false

# ----------------------------------------------------------------------------
# nat-t-keep-alive  (occurs in: nat-t-config)
# ----------------------------------------------------------------------------
[element.nat-t-keep-alive]
kind        = leaf
# learned: 2 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# nat-t-to-port  (occurs in: nat-t-config)
# ----------------------------------------------------------------------------
[element.nat-t-to-port]
kind        = leaf
# learned: 2 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# nat-t-udp-checksum-enabled  (occurs in: nat-t-config)
# ----------------------------------------------------------------------------
[element.nat-t-udp-checksum-enabled]
kind        = leaf
# learned: 2 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# netflow  (occurs in: interface)
# ----------------------------------------------------------------------------
[element.netflow]
kind        = leaf
# learned: 5 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# netmask  (occurs in: physical-if)
# ----------------------------------------------------------------------------
[element.netmask]
kind        = leaf
# learned: 5 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# network-unreachable  (occurs in: icmp-error-handling)
# ----------------------------------------------------------------------------
[element.network-unreachable]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# non-compliant-drop  (occurs in: policy, settings)
# ----------------------------------------------------------------------------
[element.non-compliant-drop]
kind        = leaf
# learned: 16 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# non-inet-class  (occurs in: request)
# ----------------------------------------------------------------------------
[element.non-inet-class]
kind        = record
# learned: 2 instance(s) in reference
allowed_fields  = deny, log

# ----------------------------------------------------------------------------
# not-scored  (occurs in: spam)
# ----------------------------------------------------------------------------
[element.not-scored]
kind        = record
# learned: 10 instance(s) in reference
required_fields = allow, log
allowed_fields  = allow, log

# ----------------------------------------------------------------------------
# notification-enabled  (occurs in: settings)
# ----------------------------------------------------------------------------
[element.notification-enabled]
kind        = leaf
# learned: 15 instance(s) in reference
value_type      = string
allowed_values  = false
allow_empty     = false

# ----------------------------------------------------------------------------
# notification-type  (occurs in: settings)
# ----------------------------------------------------------------------------
[element.notification-type]
kind        = leaf
# learned: 8 instance(s) in reference
value_type      = string
allowed_values  = Email
allow_empty     = false

# ----------------------------------------------------------------------------
# ntp-conf  (occurs in: system-parameters)
# ----------------------------------------------------------------------------
[element.ntp-conf]
kind        = record
# learned: 1 instance(s) in reference
allowed_fields  = ntp-enabled, ntp-query-enabled, ntp-server-list

# ----------------------------------------------------------------------------
# ntp-enabled  (occurs in: ntp-conf)
# ----------------------------------------------------------------------------
[element.ntp-enabled]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# ntp-query-enabled  (occurs in: ntp-conf)
# ----------------------------------------------------------------------------
[element.ntp-query-enabled]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# ntp-server-ip  (occurs in: ntp-server-list)
# ----------------------------------------------------------------------------
[element.ntp-server-ip]
kind        = leaf
# learned: 3 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# ntp-server-list  (occurs in: ntp-conf)
# ----------------------------------------------------------------------------
[element.ntp-server-list]
kind        = container
# learned: 1 instance(s) in reference
item_element = ntp-server-ip
allow_empty  = false

# ----------------------------------------------------------------------------
# ocsp  (occurs in: tls-profile, smtp)
# ----------------------------------------------------------------------------
[element.ocsp]
kind        = record
# learned: 8 instance(s) in reference
required_fields = active, unknown_handling
allowed_fields  = active, unknown_handling

# ----------------------------------------------------------------------------
# on-flag  (occurs in: member)
# ----------------------------------------------------------------------------
[element.on-flag]
kind        = leaf
# learned: 6 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# on-off  (occurs in: schedule)
# ----------------------------------------------------------------------------
[element.on-off]
kind        = leaf
# learned: 2 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# one-to-one-nat-enabled  (occurs in: local-remote-pair)
# ----------------------------------------------------------------------------
[element.one-to-one-nat-enabled]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# one-to-one-nat-list  (occurs in: profile)
# ----------------------------------------------------------------------------
[element.one-to-one-nat-list]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# op  (occurs in: alarm-action)
# ----------------------------------------------------------------------------
[element.op]
kind        = leaf
# learned: 2 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# opcodes  (occurs in: request)
# ----------------------------------------------------------------------------
[element.opcodes]
kind        = record
# learned: 2 instance(s) in reference
allowed_fields  = fallthrough, rule
repeating_fields = rule

# ----------------------------------------------------------------------------
# others-sticky-timer  (occurs in: multi-wan)
# ----------------------------------------------------------------------------
[element.others-sticky-timer]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# outgoing  (occurs in: proxy-action)
# ----------------------------------------------------------------------------
[element.outgoing]
kind        = record
# learned: 2 instance(s) in reference
allowed_fields  = apps-block-profile, ips, log, protocols, redirect-imaps, redirect-pop3s, redirect-smtps

# ----------------------------------------------------------------------------
# override  (occurs in: dpi-exception, webblocker)
# ----------------------------------------------------------------------------
[element.override]
kind        = ambiguous
# learned: 7 instance(s) in reference

# ----------------------------------------------------------------------------
# override-id-list  (occurs in: https-exception-override)
# ----------------------------------------------------------------------------
[element.override-id-list]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# pad  (occurs in: envelope)
# ----------------------------------------------------------------------------
[element.pad]
kind        = container
# learned: 4 instance(s) in reference
item_element = action
allow_empty  = false

# ----------------------------------------------------------------------------
# passphrase  (occurs in: abs-muvpn)
# ----------------------------------------------------------------------------
[element.passphrase]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# password  (occurs in: limits, protocol, radius-conf)
# ----------------------------------------------------------------------------
[element.password]
kind        = record
# learned: 16 instance(s) in reference
allowed_fields  = action, allow, log, value

# ----------------------------------------------------------------------------
# path  (occurs in: uri)
# ----------------------------------------------------------------------------
[element.path]
kind        = container
# learned: 7 instance(s) in reference
item_element = rules
allow_empty  = false

# ----------------------------------------------------------------------------
# pattern  (occurs in: rule)
# ----------------------------------------------------------------------------
[element.pattern]
kind        = leaf
# learned: 1016 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# peer-addr  (occurs in: ike-policy)
# ----------------------------------------------------------------------------
[element.peer-addr]
kind        = leaf
# learned: 3 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# peer-auth  (occurs in: ike-policy)
# ----------------------------------------------------------------------------
[element.peer-auth]
kind        = record
# learned: 3 instance(s) in reference
allowed_fields  = ip-addr, peer-auth-mask

# ----------------------------------------------------------------------------
# peer-auth-mask  (occurs in: peer-auth)
# ----------------------------------------------------------------------------
[element.peer-auth-mask]
kind        = leaf
# learned: 3 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# performance-log  (occurs in: system-parameters)
# ----------------------------------------------------------------------------
[element.performance-log]
kind        = container
# learned: 1 instance(s) in reference
item_element = bandwidth
allow_empty  = false

# ----------------------------------------------------------------------------
# period  (occurs in: member)
# ----------------------------------------------------------------------------
[element.period]
kind        = container
# learned: 6 instance(s) in reference
item_element = member
allow_empty  = false

# ----------------------------------------------------------------------------
# pfs  (occurs in: ipsec-action, ike-action)
# ----------------------------------------------------------------------------
[element.pfs]
kind        = leaf
# learned: 4 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# physical-if  (occurs in: item)
# ----------------------------------------------------------------------------
[element.physical-if]
kind        = record
# learned: 5 instance(s) in reference
allowed_fields  = anti-scan, anti-spoof, auto-negotiation, block-notification, default-gateway, dhcp-server, dos-prevention, enabled, external-if, full-duplex, if-dev-name, if-num, if-property, intra-inspection, ip, link-speed, mac-address, mac-address-enable, mtu, netmask, qos, secondary-ip-list, static-mac-acl, static-mac-ip-binds, vpn-aging-pmtu, vpn-df-bit, vpn-min-pmtu

# ----------------------------------------------------------------------------
# pipelining  (occurs in: protocol)
# ----------------------------------------------------------------------------
[element.pipelining]
kind        = container
# learned: 7 instance(s) in reference
item_element = backlog
allow_empty  = false

# ----------------------------------------------------------------------------
# pmtu-discovery  (occurs in: icmp-error-handling)
# ----------------------------------------------------------------------------
[element.pmtu-discovery]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# policy  (occurs in: policy-list)
# ----------------------------------------------------------------------------
[element.policy]
kind        = ambiguous
# learned: 31 instance(s) in reference

# ----------------------------------------------------------------------------
# policy-domain-list  (occurs in: gateway-wireless-controller)
# ----------------------------------------------------------------------------
[element.policy-domain-list]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# policy-filter-list  (occurs in: profile)
# ----------------------------------------------------------------------------
[element.policy-filter-list]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# policy-list  (occurs in: abs-policy, profile)
# ----------------------------------------------------------------------------
[element.policy-list]
kind        = container
# learned: 16 instance(s) in reference
item_element = policy
allow_empty  = false

# ----------------------------------------------------------------------------
# policy-nat  (occurs in: abs-policy)
# ----------------------------------------------------------------------------
[element.policy-nat]
kind        = leaf
# learned: 3 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# policy-routing  (occurs in: settings, policy)
# ----------------------------------------------------------------------------
[element.policy-routing]
kind        = leaf
# learned: 15 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# policy-sticky-timer  (occurs in: policy, settings)
# ----------------------------------------------------------------------------
[element.policy-sticky-timer]
kind        = leaf
# learned: 31 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# policy-tag-list  (occurs in: profile)
# ----------------------------------------------------------------------------
[element.policy-tag-list]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# policy-view  (occurs in: profile)
# ----------------------------------------------------------------------------
[element.policy-view]
kind        = record
# learned: 1 instance(s) in reference
allowed_fields  = ui-pm, view

# ----------------------------------------------------------------------------
# pop3  (occurs in: proxy-action)
# ----------------------------------------------------------------------------
[element.pop3]
kind        = record
# learned: 4 instance(s) in reference
allowed_fields  = anti-virus, ips, message, protocol, transaction

# ----------------------------------------------------------------------------
# pop3s  (occurs in: message)
# ----------------------------------------------------------------------------
[element.pop3s]
kind        = record
# learned: 4 instance(s) in reference
allowed_fields  = action, tls-profile

# ----------------------------------------------------------------------------
# port  (occurs in: redirect, radius-conf, log-server)
# ----------------------------------------------------------------------------
[element.port]
kind        = leaf
# learned: 9 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# port-type  (occurs in: route-entry)
# ----------------------------------------------------------------------------
[element.port-type]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# port-unreachable  (occurs in: icmp-error-handling)
# ----------------------------------------------------------------------------
[element.port-unreachable]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# ports  (occurs in: connect)
# ----------------------------------------------------------------------------
[element.ports]
kind        = record
# learned: 1 instance(s) in reference
allowed_fields  = fallthrough, rule

# ----------------------------------------------------------------------------
# prf-algm  (occurs in: member)
# ----------------------------------------------------------------------------
[element.prf-algm]
kind        = leaf
# learned: 5 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# primary-server  (occurs in: dvcp-client)
# ----------------------------------------------------------------------------
[element.primary-server]
kind        = record
# learned: 1 instance(s) in reference
allowed_fields  = ip, password

# ----------------------------------------------------------------------------
# priority-method  (occurs in: qos-marking)
# ----------------------------------------------------------------------------
[element.priority-method]
kind        = leaf
# learned: 36 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# proactive-patterns-enabled  (occurs in: connection-string)
# ----------------------------------------------------------------------------
[element.proactive-patterns-enabled]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# probe-list  (occurs in: profile)
# ----------------------------------------------------------------------------
[element.probe-list]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# product-grade  (occurs in: profile)
# ----------------------------------------------------------------------------
[element.product-grade]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# property  (occurs in: service, alias, alarm-action)
# ----------------------------------------------------------------------------
[element.property]
kind        = leaf
# learned: 342 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# protection-type  (occurs in: ips)
# ----------------------------------------------------------------------------
[element.protection-type]
kind        = leaf
# learned: 24 instance(s) in reference
value_type      = string
allowed_values  = client, server
allow_empty     = false

# ----------------------------------------------------------------------------
# protocol  (occurs in: member, request, pop3)
# ----------------------------------------------------------------------------
[element.protocol]
kind        = ambiguous
# learned: 171 instance(s) in reference

# ----------------------------------------------------------------------------
# protocol-unreachable  (occurs in: icmp-error-handling)
# ----------------------------------------------------------------------------
[element.protocol-unreachable]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# protocols  (occurs in: outgoing)
# ----------------------------------------------------------------------------
[element.protocols]
kind        = record
# learned: 2 instance(s) in reference
allowed_fields  = fallthrough, rule
repeating_fields = rule

# ----------------------------------------------------------------------------
# proxy  (occurs in: policy, settings)
# ----------------------------------------------------------------------------
[element.proxy]
kind        = leaf
# learned: 31 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# proxy-access  (occurs in: connection-string, signature-update)
# ----------------------------------------------------------------------------
[element.proxy-access]
kind        = leaf
# learned: 2 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# proxy-action  (occurs in: proxy-action-list)
# ----------------------------------------------------------------------------
[element.proxy-action]
kind        = record
# learned: 33 instance(s) in reference
required_fields = property, proxy-description, proxy-name, proxy-type, proxy-type-attr
allowed_fields  = diag-log-level, dns, ftp, h323, http, https, imap, outgoing, pop3, property, proxy-description, proxy-name, proxy-referenced, proxy-type, proxy-type-attr, sip, smtp

# ----------------------------------------------------------------------------
# proxy-action-list  (occurs in: profile)
# ----------------------------------------------------------------------------
[element.proxy-action-list]
kind        = container
# learned: 1 instance(s) in reference
item_element = proxy-action
allow_empty  = false

# ----------------------------------------------------------------------------
# proxy-arp  (occurs in: nat)
# ----------------------------------------------------------------------------
[element.proxy-arp]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# proxy-auth  (occurs in: connection-string, signature-update)
# ----------------------------------------------------------------------------
[element.proxy-auth]
kind        = leaf
# learned: 2 instance(s) in reference
value_type      = string
allowed_values  = NoAuth
allow_empty     = false

# ----------------------------------------------------------------------------
# proxy-description  (occurs in: proxy-action, proxy-helper)
# ----------------------------------------------------------------------------
[element.proxy-description]
kind        = leaf
# learned: 38 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# proxy-helper  (occurs in: proxy-helper-list)
# ----------------------------------------------------------------------------
[element.proxy-helper]
kind        = record
# learned: 5 instance(s) in reference
required_fields = property, proxy-description, proxy-name, proxy-type
allowed_fields  = ctspam, diag-log-level, property, proxy-description, proxy-name, proxy-referenced, proxy-type, quarantine, sigeng, webblocker

# ----------------------------------------------------------------------------
# proxy-helper-list  (occurs in: profile)
# ----------------------------------------------------------------------------
[element.proxy-helper-list]
kind        = container
# learned: 1 instance(s) in reference
item_element = proxy-helper
allow_empty  = false

# ----------------------------------------------------------------------------
# proxy-name  (occurs in: proxy-action, proxy-helper)
# ----------------------------------------------------------------------------
[element.proxy-name]
kind        = leaf
# learned: 38 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# proxy-password  (occurs in: connection-string, signature-update)
# ----------------------------------------------------------------------------
[element.proxy-password]
kind        = leaf
# learned: 2 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# proxy-port  (occurs in: connection-string, signature-update)
# ----------------------------------------------------------------------------
[element.proxy-port]
kind        = leaf
# learned: 2 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# proxy-referenced  (occurs in: proxy-action, proxy-helper)
# ----------------------------------------------------------------------------
[element.proxy-referenced]
kind        = leaf
# learned: 34 instance(s) in reference
value_type      = string
allowed_values  = false
allow_empty     = false

# ----------------------------------------------------------------------------
# proxy-server-address  (occurs in: connection-string, signature-update)
# ----------------------------------------------------------------------------
[element.proxy-server-address]
kind        = leaf
# learned: 2 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# proxy-type  (occurs in: service, proxy-action, proxy-helper)
# ----------------------------------------------------------------------------
[element.proxy-type]
kind        = leaf
# learned: 139 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# proxy-type-attr  (occurs in: proxy-action, tls-profile)
# ----------------------------------------------------------------------------
[element.proxy-type-attr]
kind        = leaf
# learned: 37 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# proxy-user-domain  (occurs in: connection-string, signature-update)
# ----------------------------------------------------------------------------
[element.proxy-user-domain]
kind        = leaf
# learned: 2 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# proxy-user-name  (occurs in: connection-string, signature-update)
# ----------------------------------------------------------------------------
[element.proxy-user-name]
kind        = leaf
# learned: 2 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# psk  (occurs in: local-cert)
# ----------------------------------------------------------------------------
[element.psk]
kind        = leaf
# learned: 3 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# psk-hex  (occurs in: local-cert)
# ----------------------------------------------------------------------------
[element.psk-hex]
kind        = leaf
# learned: 3 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# qos  (occurs in: physical-if)
# ----------------------------------------------------------------------------
[element.qos]
kind        = record
# learned: 5 instance(s) in reference
required_fields = max-link-bandwidth, qos-marking
allowed_fields  = max-link-bandwidth, qos-marking

# ----------------------------------------------------------------------------
# qos-enable  (occurs in: misc-global-setting)
# ----------------------------------------------------------------------------
[element.qos-enable]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# qos-marking  (occurs in: policy, settings, qos)
# ----------------------------------------------------------------------------
[element.qos-marking]
kind        = record
# learned: 36 instance(s) in reference
required_fields = marking-field, marking-method, priority-method
allowed_fields  = marking-field, marking-method, priority-method

# ----------------------------------------------------------------------------
# quarantine  (occurs in: proxy-helper)
# ----------------------------------------------------------------------------
[element.quarantine]
kind        = record
# learned: 1 instance(s) in reference
allowed_fields  = address, port

# ----------------------------------------------------------------------------
# query-names  (occurs in: request)
# ----------------------------------------------------------------------------
[element.query-names]
kind        = record
# learned: 2 instance(s) in reference
allowed_fields  = fallthrough, rule
repeating_fields = rule

# ----------------------------------------------------------------------------
# query-types  (occurs in: request)
# ----------------------------------------------------------------------------
[element.query-types]
kind        = record
# learned: 2 instance(s) in reference
allowed_fields  = fallthrough, rule
repeating_fields = rule

# ----------------------------------------------------------------------------
# quota-enabled  (occurs in: policy, settings)
# ----------------------------------------------------------------------------
[element.quota-enabled]
kind        = leaf
# learned: 16 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# radius-conf  (occurs in: radius-conf-list)
# ----------------------------------------------------------------------------
[element.radius-conf]
kind        = record
# learned: 2 instance(s) in reference
allowed_fields  = ip, name, password, port

# ----------------------------------------------------------------------------
# radius-conf-list  (occurs in: profile)
# ----------------------------------------------------------------------------
[element.radius-conf-list]
kind        = container
# learned: 1 instance(s) in reference
item_element = radius-conf
allow_empty  = false

# ----------------------------------------------------------------------------
# range  (occurs in: request)
# ----------------------------------------------------------------------------
[element.range]
kind        = record
# learned: 7 instance(s) in reference
required_fields = enabled, log
allowed_fields  = enabled, log

# ----------------------------------------------------------------------------
# ras-domain-item-list  (occurs in: profile)
# ----------------------------------------------------------------------------
[element.ras-domain-item-list]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# ras-server-group-list  (occurs in: profile)
# ----------------------------------------------------------------------------
[element.ras-server-group-list]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# ras-user-group  (occurs in: ras-user-group-list)
# ----------------------------------------------------------------------------
[element.ras-user-group]
kind        = record
# learned: 1 instance(s) in reference
allowed_fields  = addr-grp-name, addr-mgmt, auth-domain, auth-domain-list, description, dns-mgmt, eap-method, expiry, global-idle-timeout, name, property, version

# ----------------------------------------------------------------------------
# ras-user-group-list  (occurs in: profile)
# ----------------------------------------------------------------------------
[element.ras-user-group-list]
kind        = container
# learned: 1 instance(s) in reference
item_element = ras-user-group
allow_empty  = false

# ----------------------------------------------------------------------------
# ras-user-group-name  (occurs in: ike-action)
# ----------------------------------------------------------------------------
[element.ras-user-group-name]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# ras-user-list  (occurs in: profile)
# ----------------------------------------------------------------------------
[element.ras-user-list]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# readonly  (occurs in: protocol)
# ----------------------------------------------------------------------------
[element.readonly]
kind        = leaf
# learned: 4 instance(s) in reference
value_type      = string
allowed_values  = false
allow_empty     = false

# ----------------------------------------------------------------------------
# reason  (occurs in: exception-ip)
# ----------------------------------------------------------------------------
[element.reason]
kind        = leaf
# learned: 32 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# recipient  (occurs in: rule)
# ----------------------------------------------------------------------------
[element.recipient]
kind        = leaf
# learned: 12 instance(s) in reference
value_type      = string
allowed_values  = *
allow_empty     = false

# ----------------------------------------------------------------------------
# recursion  (occurs in: request)
# ----------------------------------------------------------------------------
[element.recursion]
kind        = record
# learned: 2 instance(s) in reference
allowed_fields  = allow, log

# ----------------------------------------------------------------------------
# redirect  (occurs in: http)
# ----------------------------------------------------------------------------
[element.redirect]
kind        = record
# learned: 4 instance(s) in reference
allowed_fields  = address, enabled, port

# ----------------------------------------------------------------------------
# redirect-imaps  (occurs in: outgoing)
# ----------------------------------------------------------------------------
[element.redirect-imaps]
kind        = leaf
# learned: 2 instance(s) in reference
value_type      = string
allowed_values  = true
allow_empty     = false

# ----------------------------------------------------------------------------
# redirect-pop3s  (occurs in: outgoing)
# ----------------------------------------------------------------------------
[element.redirect-pop3s]
kind        = leaf
# learned: 2 instance(s) in reference
value_type      = string
allowed_values  = true
allow_empty     = false

# ----------------------------------------------------------------------------
# redirect-smtps  (occurs in: outgoing)
# ----------------------------------------------------------------------------
[element.redirect-smtps]
kind        = leaf
# learned: 2 instance(s) in reference
value_type      = string
allowed_values  = true
allow_empty     = false

# ----------------------------------------------------------------------------
# redirect-to  (occurs in: https, ftp)
# ----------------------------------------------------------------------------
[element.redirect-to]
kind        = leaf
# learned: 6 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# redirect-url  (occurs in: auth-global-setting)
# ----------------------------------------------------------------------------
[element.redirect-url]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# regexp  (occurs in: rule)
# ----------------------------------------------------------------------------
[element.regexp]
kind        = leaf
# learned: 13 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# reject-action  (occurs in: policy, abs-policy)
# ----------------------------------------------------------------------------
[element.reject-action]
kind        = leaf
# learned: 31 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# remote-action  (occurs in: alarm-action)
# ----------------------------------------------------------------------------
[element.remote-action]
kind        = container
# learned: 55 instance(s) in reference
item_element = action-type
allow_empty  = false

# ----------------------------------------------------------------------------
# remote-addr  (occurs in: local-remote-pair)
# ----------------------------------------------------------------------------
[element.remote-addr]
kind        = ambiguous
# learned: 3 instance(s) in reference

# ----------------------------------------------------------------------------
# remote-enable  (occurs in: alarm-action)
# ----------------------------------------------------------------------------
[element.remote-enable]
kind        = leaf
# learned: 55 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# repeat-count  (occurs in: alarm-action, settings)
# ----------------------------------------------------------------------------
[element.repeat-count]
kind        = leaf
# learned: 63 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# replace  (occurs in: rule, bulk, confirmed)
# ----------------------------------------------------------------------------
[element.replace]
kind        = leaf
# learned: 37 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# reputation-authority  (occurs in: http)
# ----------------------------------------------------------------------------
[element.reputation-authority]
kind        = record
# learned: 1 instance(s) in reference
allowed_fields  = block, bypass

# ----------------------------------------------------------------------------
# request  (occurs in: http, ftp, dns)
# ----------------------------------------------------------------------------
[element.request]
kind        = record
# learned: 14 instance(s) in reference
required_fields = timeout
allowed_fields  = commands, download, errors, header, limits, log, method, non-inet-class, opcodes, protocol, query-names, query-types, range, recursion, safe-search, safe-search-level, timeout, upload, uri

# ----------------------------------------------------------------------------
# resign  (occurs in: sslfilter)
# ----------------------------------------------------------------------------
[element.resign]
kind        = leaf
# learned: 9 instance(s) in reference
value_type      = string
allowed_values  = SSL_RESIGN_NO, SSL_RESIGN_YES
allow_empty     = false

# ----------------------------------------------------------------------------
# resource-addr  (occurs in: resource-addr-list)
# ----------------------------------------------------------------------------
[element.resource-addr]
kind        = container
# learned: 1 instance(s) in reference
item_element = ip-network-addr
allow_empty  = false

# ----------------------------------------------------------------------------
# resource-addr-list  (occurs in: abs-muvpn)
# ----------------------------------------------------------------------------
[element.resource-addr-list]
kind        = container
# learned: 1 instance(s) in reference
item_element = resource-addr
allow_empty  = false

# ----------------------------------------------------------------------------
# response  (occurs in: http, dns)
# ----------------------------------------------------------------------------
[element.response]
kind        = record
# learned: 9 instance(s) in reference
required_fields = timeout
allowed_fields  = body, header, timeout

# ----------------------------------------------------------------------------
# restrict-traffic  (occurs in: static-mac-ip-binds)
# ----------------------------------------------------------------------------
[element.restrict-traffic]
kind        = leaf
# learned: 5 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# reverse-traffic-mgmt  (occurs in: policy, settings)
# ----------------------------------------------------------------------------
[element.reverse-traffic-mgmt]
kind        = leaf
# learned: 16 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# revision  (occurs in: revision-history)
# ----------------------------------------------------------------------------
[element.revision]
kind        = record
# learned: 1 instance(s) in reference
allowed_fields  = comment, module-id, time

# ----------------------------------------------------------------------------
# revision-history  (occurs in: profile)
# ----------------------------------------------------------------------------
[element.revision-history]
kind        = container
# learned: 1 instance(s) in reference
item_element = revision
allow_empty  = false

# ----------------------------------------------------------------------------
# route  (occurs in: system-parameters)
# ----------------------------------------------------------------------------
[element.route]
kind        = container
# learned: 1 instance(s) in reference
item_element = route-entry
allow_empty  = false

# ----------------------------------------------------------------------------
# route-entry  (occurs in: route)
# ----------------------------------------------------------------------------
[element.route-entry]
kind        = record
# learned: 1 instance(s) in reference
allowed_fields  = card-id, dest-address, gateway-ip, mask, metric, port-type

# ----------------------------------------------------------------------------
# routing-protocol-list  (occurs in: dynamic-routing)
# ----------------------------------------------------------------------------
[element.routing-protocol-list]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# rp-candidate  (occurs in: multicast-if)
# ----------------------------------------------------------------------------
[element.rp-candidate]
kind        = leaf
# learned: 5 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# rs-version  (occurs in: profile)
# ----------------------------------------------------------------------------
[element.rs-version]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# rsa-cert  (occurs in: local-cert)
# ----------------------------------------------------------------------------
[element.rsa-cert]
kind        = leaf
# learned: 3 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# rsa-id  (occurs in: local-cert)
# ----------------------------------------------------------------------------
[element.rsa-id]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# rsa-id-type  (occurs in: local-cert)
# ----------------------------------------------------------------------------
[element.rsa-id-type]
kind        = leaf
# learned: 3 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# rss-check  (occurs in: lsd)
# ----------------------------------------------------------------------------
[element.rss-check]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# rss-url  (occurs in: lsd)
# ----------------------------------------------------------------------------
[element.rss-url]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# rule  (occurs in: rules, headers, file-patterns)
# ----------------------------------------------------------------------------
[element.rule]
kind        = record
# learned: 1471 instance(s) in reference
allowed_fields  = alarm, allow, deny, enabled, log, name, pattern, recipient, regexp, replace, sender, string, strip, value

# ----------------------------------------------------------------------------
# rules  (occurs in: content-type, version, method)
# ----------------------------------------------------------------------------
[element.rules]
kind        = record
# learned: 75 instance(s) in reference
required_fields = fallthrough
allowed_fields  = fallthrough, match-length, rule
repeating_fields = rule

# ----------------------------------------------------------------------------
# safe-search  (occurs in: request)
# ----------------------------------------------------------------------------
[element.safe-search]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# safe-search-level  (occurs in: request)
# ----------------------------------------------------------------------------
[element.safe-search-level]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# scan-limit  (occurs in: anti-virus, dlp-sensor)
# ----------------------------------------------------------------------------
[element.scan-limit]
kind        = leaf
# learned: 24 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# scan-mode  (occurs in: antivirus)
# ----------------------------------------------------------------------------
[element.scan-mode]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# schedule  (occurs in: policy, settings, schedule-list)
# ----------------------------------------------------------------------------
[element.schedule]
kind        = ambiguous
# learned: 35 instance(s) in reference

# ----------------------------------------------------------------------------
# schedule-list  (occurs in: profile)
# ----------------------------------------------------------------------------
[element.schedule-list]
kind        = container
# learned: 1 instance(s) in reference
item_element = schedule
allow_empty  = false

# ----------------------------------------------------------------------------
# secondary-ip-list  (occurs in: physical-if)
# ----------------------------------------------------------------------------
[element.secondary-ip-list]
kind        = leaf
# learned: 5 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# send-tcp-reset  (occurs in: policy)
# ----------------------------------------------------------------------------
[element.send-tcp-reset]
kind        = leaf
# learned: 16 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# sender  (occurs in: rule)
# ----------------------------------------------------------------------------
[element.sender]
kind        = leaf
# learned: 12 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# serial  (occurs in: lsd)
# ----------------------------------------------------------------------------
[element.serial]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# server  (occurs in: sslfilter)
# ----------------------------------------------------------------------------
[element.server]
kind        = record
# learned: 13 instance(s) in reference
required_fields = ecdhe
allowed_fields  = ecdhe, type

# ----------------------------------------------------------------------------
# server-address  (occurs in: connection-string)
# ----------------------------------------------------------------------------
[element.server-address]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# server-bypass  (occurs in: webblocker)
# ----------------------------------------------------------------------------
[element.server-bypass]
kind        = leaf
# learned: 2 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# server-bypass-action  (occurs in: webblocker)
# ----------------------------------------------------------------------------
[element.server-bypass-action]
kind        = record
# learned: 2 instance(s) in reference
allowed_fields  = alarm, allow, log

# ----------------------------------------------------------------------------
# server-fallthrough-option  (occurs in: transport)
# ----------------------------------------------------------------------------
[element.server-fallthrough-option]
kind        = leaf
# learned: 4 instance(s) in reference
value_type      = string
allowed_values  = not_forbidden
allow_empty     = false

# ----------------------------------------------------------------------------
# server-list  (occurs in: webblocker)
# ----------------------------------------------------------------------------
[element.server-list]
kind        = leaf
# learned: 2 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# server-port  (occurs in: member, dvcp-client)
# ----------------------------------------------------------------------------
[element.server-port]
kind        = leaf
# learned: 153 instance(s) in reference
# Tightened 2026-06-03: WatchGuard's Port-Type is numeric [0, 65535].
# Lab T-30 12.5.9 rejected our import with line 1348:
#   "Element 'server-port': 'echo-reply' is not a valid value of
#    the atomic type 'Port-Type'."
# Server-port=0 is a valid placeholder for layer-3 protocols.
value_type      = int
allow_empty     = false

# ----------------------------------------------------------------------------
# server-replies  (occurs in: masquerading, masquerade)
# ----------------------------------------------------------------------------
[element.server-replies]
kind        = leaf
# learned: 8 instance(s) in reference
value_type      = string
allowed_values  = true
allow_empty     = false

# ----------------------------------------------------------------------------
# server-timeout  (occurs in: webblocker)
# ----------------------------------------------------------------------------
[element.server-timeout]
kind        = leaf
# learned: 2 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# server-timeout-action  (occurs in: webblocker)
# ----------------------------------------------------------------------------
[element.server-timeout-action]
kind        = record
# learned: 2 instance(s) in reference
allowed_fields  = alarm, allow, deny, log

# ----------------------------------------------------------------------------
# server-type  (occurs in: dhcp-server)
# ----------------------------------------------------------------------------
[element.server-type]
kind        = leaf
# learned: 2 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# server-url  (occurs in: signature-update)
# ----------------------------------------------------------------------------
[element.server-url]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# service  (occurs in: service-list, policy, abs-policy)
# ----------------------------------------------------------------------------
[element.service]
kind        = ambiguous
# learned: 137 instance(s) in reference

# ----------------------------------------------------------------------------
# service-item  (occurs in: service)
# ----------------------------------------------------------------------------
[element.service-item]
kind        = container
# learned: 106 instance(s) in reference
item_element = member
allow_empty  = false

# ----------------------------------------------------------------------------
# service-list  (occurs in: profile)
# ----------------------------------------------------------------------------
[element.service-list]
kind        = container
# learned: 1 instance(s) in reference
item_element = service
allow_empty  = false

# ----------------------------------------------------------------------------
# services  (occurs in: signature-update)
# ----------------------------------------------------------------------------
[element.services]
kind        = record
# learned: 1 instance(s) in reference
allowed_fields  = Engine, GAV, IAV, IPS

# ----------------------------------------------------------------------------
# session-idle-timeout  (occurs in: terminal-service)
# ----------------------------------------------------------------------------
[element.session-idle-timeout]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# settings  (occurs in: abs-policy)
# ----------------------------------------------------------------------------
[element.settings]
kind        = record
# learned: 15 instance(s) in reference
required_fields = apply-dnat-all-traffic, apply-dnat-global, apply-one-to-one-nat-rules, auto-block-enabled, connection-rate, dnat-src-ip, idle-timeout, ips-monitor-enabled, log-enabled, notification-enabled, policy-sticky-timer, proxy, qos-marking, schedule, snmp-enabled, using-global-sticky-setting
allowed_fields  = apply-dnat-all-traffic, apply-dnat-global, apply-one-to-one-nat-rules, auto-block-enabled, compliance-check, connection-rate, connection-rate-alarm, dnat-src-ip, forward-traffic-mgmt, geo-action, idle-timeout, ips-monitor-enabled, launch-interval, log-enabled, log-for-report, non-compliant-drop, notification-enabled, notification-type, policy-routing, policy-sticky-timer, proxy, qos-marking, quota-enabled, repeat-count, reverse-traffic-mgmt, schedule, snmp-enabled, traffic-mgmt, using-global-sticky-setting

# ----------------------------------------------------------------------------
# severity  (occurs in: alarm-action)
# ----------------------------------------------------------------------------
[element.severity]
kind        = leaf
# learned: 49 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# sharing-across-policy  (occurs in: vpn-tunnel-sharing)
# ----------------------------------------------------------------------------
[element.sharing-across-policy]
kind        = leaf
# learned: 2 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# sigeng  (occurs in: proxy-helper)
# ----------------------------------------------------------------------------
[element.sigeng]
kind        = record
# learned: 1 instance(s) in reference
allowed_fields  = apps, archives, av, depth, ips, ips-signature-exception-list

# ----------------------------------------------------------------------------
# signature-update  (occurs in: profile)
# ----------------------------------------------------------------------------
[element.signature-update]
kind        = record
# learned: 1 instance(s) in reference
allowed_fields  = auto-interval, proxy-access, proxy-auth, proxy-password, proxy-port, proxy-server-address, proxy-user-domain, proxy-user-name, server-url, services

# ----------------------------------------------------------------------------
# single-sign-on  (occurs in: system-parameters)
# ----------------------------------------------------------------------------
[element.single-sign-on]
kind        = record
# learned: 1 instance(s) in reference
allowed_fields  = enabled, exclude-ip-list, sso-agent-ip, user-ip-cache-timeout

# ----------------------------------------------------------------------------
# sip  (occurs in: proxy-action)
# ----------------------------------------------------------------------------
[element.sip]
kind        = record
# learned: 1 instance(s) in reference
allowed_fields  = acl, denied-codecs, enable-dirharvesting, enable-normalization, enable-topology-hiding, max-sessions, timeout, transaction, user-agent

# ----------------------------------------------------------------------------
# smtp  (occurs in: proxy-action)
# ----------------------------------------------------------------------------
[element.smtp]
kind        = record
# learned: 4 instance(s) in reference
allowed_fields  = anti-virus, debug, encryption, envelope, ips, masquerading, message, ocsp, smtps, sslfilter, timeout, tls-profile, transaction

# ----------------------------------------------------------------------------
# smtp-action  (occurs in: error-action, unscannable-action, too-large-action)
# ----------------------------------------------------------------------------
[element.smtp-action]
kind        = leaf
# learned: 8 instance(s) in reference
value_type      = string
allowed_values  = allow
allow_empty     = false

# ----------------------------------------------------------------------------
# smtps  (occurs in: smtp)
# ----------------------------------------------------------------------------
[element.smtps]
kind        = record
# learned: 4 instance(s) in reference
allowed_fields  = action, tls-profile

# ----------------------------------------------------------------------------
# snmp-comm-string  (occurs in: snmp-protocol)
# ----------------------------------------------------------------------------
[element.snmp-comm-string]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# snmp-conf  (occurs in: system-parameters)
# ----------------------------------------------------------------------------
[element.snmp-conf]
kind        = record
# learned: 1 instance(s) in reference
allowed_fields  = mgr-station-list, snmp-protocol, snmp-trap

# ----------------------------------------------------------------------------
# snmp-enabled  (occurs in: settings)
# ----------------------------------------------------------------------------
[element.snmp-enabled]
kind        = leaf
# learned: 15 instance(s) in reference
value_type      = string
allowed_values  = false
allow_empty     = false

# ----------------------------------------------------------------------------
# snmp-protocol  (occurs in: snmp-conf)
# ----------------------------------------------------------------------------
[element.snmp-protocol]
kind        = record
# learned: 1 instance(s) in reference
allowed_fields  = snmp-comm-string, snmp-version

# ----------------------------------------------------------------------------
# snmp-trap  (occurs in: snmp-conf)
# ----------------------------------------------------------------------------
[element.snmp-trap]
kind        = record
# learned: 1 instance(s) in reference
allowed_fields  = snmp-trap-type, snmp-trap-version

# ----------------------------------------------------------------------------
# snmp-trap-type  (occurs in: snmp-trap)
# ----------------------------------------------------------------------------
[element.snmp-trap-type]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# snmp-trap-version  (occurs in: snmp-trap)
# ----------------------------------------------------------------------------
[element.snmp-trap-version]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# snmp-version  (occurs in: snmp-protocol)
# ----------------------------------------------------------------------------
[element.snmp-version]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# source  (occurs in: filter)
# ----------------------------------------------------------------------------
[element.source]
kind        = record
# learned: 2 instance(s) in reference
allowed_fields  = any, type

# ----------------------------------------------------------------------------
# spam  (occurs in: message)
# ----------------------------------------------------------------------------
[element.spam]
kind        = record
# learned: 10 instance(s) in reference
required_fields = bulk, clean, confirmed, enabled, exceptions, helper, log-exception, not-scored, suspect
allowed_fields  = bulk, clean, confirmed, enabled, exceptions, helper, log-exception, not-scored, suspect

# ----------------------------------------------------------------------------
# spam-threshold  (occurs in: ctspam)
# ----------------------------------------------------------------------------
[element.spam-threshold]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# spyware  (occurs in: ips, system-parameters)
# ----------------------------------------------------------------------------
[element.spyware]
kind        = ambiguous
# learned: 10 instance(s) in reference

# ----------------------------------------------------------------------------
# ssid-list  (occurs in: gateway-wireless-controller)
# ----------------------------------------------------------------------------
[element.ssid-list]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# sslfilter  (occurs in: https, tls-profile, smtp)
# ----------------------------------------------------------------------------
[element.sslfilter]
kind        = record
# learned: 13 instance(s) in reference
required_fields = active, client, server
allowed_fields  = active, client, resign, server

# ----------------------------------------------------------------------------
# sslvpn-list  (occurs in: profile)
# ----------------------------------------------------------------------------
[element.sslvpn-list]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# sso-agent-ip  (occurs in: single-sign-on)
# ----------------------------------------------------------------------------
[element.sso-agent-ip]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# staging-url  (occurs in: lsd)
# ----------------------------------------------------------------------------
[element.staging-url]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# start-server-port  (occurs in: member)
# ----------------------------------------------------------------------------
[element.start-server-port]
kind        = leaf
# learned: 4 instance(s) in reference
value_type      = int
allow_empty     = false

# ----------------------------------------------------------------------------
# starttls  (occurs in: protocol)
# ----------------------------------------------------------------------------
[element.starttls]
kind        = record
# learned: 2 instance(s) in reference
allowed_fields  = enabled, tls-profile

# ----------------------------------------------------------------------------
# static-arp-list  (occurs in: system-parameters)
# ----------------------------------------------------------------------------
[element.static-arp-list]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# static-bind-ip-list  (occurs in: system-parameters)
# ----------------------------------------------------------------------------
[element.static-bind-ip-list]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# static-mac-acl  (occurs in: physical-if)
# ----------------------------------------------------------------------------
[element.static-mac-acl]
kind        = container
# learned: 5 instance(s) in reference
item_element = enable
allow_empty  = false

# ----------------------------------------------------------------------------
# static-mac-ip-binds  (occurs in: physical-if)
# ----------------------------------------------------------------------------
[element.static-mac-ip-binds]
kind        = container
# learned: 5 instance(s) in reference
item_element = restrict-traffic
allow_empty  = false

# ----------------------------------------------------------------------------
# string  (occurs in: rule)
# ----------------------------------------------------------------------------
[element.string]
kind        = leaf
# learned: 374 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# strip  (occurs in: rule, fallthrough, virus-found)
# ----------------------------------------------------------------------------
[element.strip]
kind        = leaf
# learned: 180 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# support-cache-enabled  (occurs in: log-conf)
# ----------------------------------------------------------------------------
[element.support-cache-enabled]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# suspect  (occurs in: spam)
# ----------------------------------------------------------------------------
[element.suspect]
kind        = record
# learned: 10 instance(s) in reference
required_fields = log
allowed_fields  = allow, log, replace

# ----------------------------------------------------------------------------
# syn-checking-enable  (occurs in: misc-global-setting)
# ----------------------------------------------------------------------------
[element.syn-checking-enable]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# system-contact  (occurs in: device-conf)
# ----------------------------------------------------------------------------
[element.system-contact]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# system-name  (occurs in: device-conf)
# ----------------------------------------------------------------------------
[element.system-name]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# system-parameters  (occurs in: profile)
# ----------------------------------------------------------------------------
[element.system-parameters]
kind        = record
# learned: 1 instance(s) in reference
allowed_fields  = antivirus, apt, auth-global-setting, blocked-ip-exception-list, blocked-ip-list, blocked-ports, botnet-detection, common-logging, ddns-service-list, device-conf, dlp, dns-server-list, dos-prevention, dvcp-client, dynamic-routing, icmp-error-handling, ips, ldap, log-conf, lsd, misc-global-setting, monitord, mss-adjustment, multi-wan, ntp-conf, performance-log, route, single-sign-on, snmp-conf, spyware, static-arp-list, static-bind-ip-list, terminal-service, trace-conf, traffic-flow-timers, vpn-global-setting, web-certificate, webblocker-global-setting, wgsync, wins-server-list

# ----------------------------------------------------------------------------
# tcp-sticky-timer  (occurs in: multi-wan)
# ----------------------------------------------------------------------------
[element.tcp-sticky-timer]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# tcp-timeout-close  (occurs in: traffic-flow-timers)
# ----------------------------------------------------------------------------
[element.tcp-timeout-close]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# tcp-timeout-close-wait  (occurs in: traffic-flow-timers)
# ----------------------------------------------------------------------------
[element.tcp-timeout-close-wait]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# tcp-timeout-established  (occurs in: traffic-flow-timers)
# ----------------------------------------------------------------------------
[element.tcp-timeout-established]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# tcp-timeout-fin-wait  (occurs in: traffic-flow-timers)
# ----------------------------------------------------------------------------
[element.tcp-timeout-fin-wait]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# tcp-timeout-last-ack  (occurs in: traffic-flow-timers)
# ----------------------------------------------------------------------------
[element.tcp-timeout-last-ack]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# tcp-timeout-max-retrans  (occurs in: traffic-flow-timers)
# ----------------------------------------------------------------------------
[element.tcp-timeout-max-retrans]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# tcp-timeout-syn-recv  (occurs in: traffic-flow-timers)
# ----------------------------------------------------------------------------
[element.tcp-timeout-syn-recv]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# tcp-timeout-syn-sent  (occurs in: traffic-flow-timers)
# ----------------------------------------------------------------------------
[element.tcp-timeout-syn-sent]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# tcp-timeout-time-wait  (occurs in: traffic-flow-timers)
# ----------------------------------------------------------------------------
[element.tcp-timeout-time-wait]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# temp-dir  (occurs in: connection-string)
# ----------------------------------------------------------------------------
[element.temp-dir]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# terminal-service  (occurs in: system-parameters)
# ----------------------------------------------------------------------------
[element.terminal-service]
kind        = record
# learned: 1 instance(s) in reference
allowed_fields  = agent-ip-list, enabled, session-idle-timeout

# ----------------------------------------------------------------------------
# text-extraction-enabled  (occurs in: dlp)
# ----------------------------------------------------------------------------
[element.text-extraction-enabled]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# threat-level  (occurs in: ips)
# ----------------------------------------------------------------------------
[element.threat-level]
kind        = record
# learned: 1 instance(s) in reference
allowed_fields  = critical, high, info, low, medium

# ----------------------------------------------------------------------------
# threshold  (occurs in: condition, bypass, block)
# ----------------------------------------------------------------------------
[element.threshold]
kind        = leaf
# learned: 16 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# time  (occurs in: revision)
# ----------------------------------------------------------------------------
[element.time]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# time-exceeded  (occurs in: icmp-error-handling)
# ----------------------------------------------------------------------------
[element.time-exceeded]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# time-unit  (occurs in: member)
# ----------------------------------------------------------------------------
[element.time-unit]
kind        = leaf
# learned: 18 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# time-zone  (occurs in: device-conf)
# ----------------------------------------------------------------------------
[element.time-zone]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# timeout  (occurs in: request, response, protocol)
# ----------------------------------------------------------------------------
[element.timeout]
kind        = ambiguous
# learned: 40 instance(s) in reference

# ----------------------------------------------------------------------------
# tls-dest-port  (occurs in: content-actions)
# ----------------------------------------------------------------------------
[element.tls-dest-port]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# tls-port  (occurs in: member)
# ----------------------------------------------------------------------------
[element.tls-port]
kind        = leaf
# learned: 3 instance(s) in reference
# NOTE: tls-port has overloaded semantics in jpa.xml — some instances
# contain port numbers, others contain 'true' as a boolean flag.
# Keeping as string until we can properly model the dual-purpose
# field. Reverted from int 2026-06-03 after jpa.xml itself failed
# int-validation on 3 'true' values.
value_type      = string
allowed_values  = true
allow_empty     = false

# ----------------------------------------------------------------------------
# tls-profile  (occurs in: https, tls-profile-list, smtp)
# ----------------------------------------------------------------------------
[element.tls-profile]
kind        = ambiguous
# learned: 25 instance(s) in reference

# ----------------------------------------------------------------------------
# tls-profile-list  (occurs in: profile)
# ----------------------------------------------------------------------------
[element.tls-profile-list]
kind        = container
# learned: 1 instance(s) in reference
item_element = tls-profile
allow_empty  = false

# ----------------------------------------------------------------------------
# to  (occurs in: addresses)
# ----------------------------------------------------------------------------
[element.to]
kind        = record
# learned: 4 instance(s) in reference
allowed_fields  = fallthrough, rule
repeating_fields = rule

# ----------------------------------------------------------------------------
# to-alias  (occurs in: dnat)
# ----------------------------------------------------------------------------
[element.to-alias]
kind        = leaf
# learned: 3 instance(s) in reference
value_type      = string
allowed_values  = Any-External
allow_empty     = false

# ----------------------------------------------------------------------------
# to-alias-list  (occurs in: policy, abs-policy)
# ----------------------------------------------------------------------------
[element.to-alias-list]
kind        = container
# learned: 31 instance(s) in reference
item_element = alias
allow_empty  = false

# ----------------------------------------------------------------------------
# to-hour  (occurs in: member)
# ----------------------------------------------------------------------------
[element.to-hour]
kind        = leaf
# learned: 6 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# to-min  (occurs in: member)
# ----------------------------------------------------------------------------
[element.to-min]
kind        = leaf
# learned: 6 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# too-large  (occurs in: anti-virus)
# ----------------------------------------------------------------------------
[element.too-large]
kind        = record
# learned: 2 instance(s) in reference
allowed_fields  = alarm, allow, log

# ----------------------------------------------------------------------------
# too-large-action  (occurs in: dlp-sensor)
# ----------------------------------------------------------------------------
[element.too-large-action]
kind        = record
# learned: 2 instance(s) in reference
allowed_fields  = action, alarm, log, smtp-action

# ----------------------------------------------------------------------------
# tos-tunnel-flag  (occurs in: vpn-global-setting)
# ----------------------------------------------------------------------------
[element.tos-tunnel-flag]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# trace-conf  (occurs in: system-parameters)
# ----------------------------------------------------------------------------
[element.trace-conf]
kind        = container
# learned: 1 instance(s) in reference
item_element = trace-item
allow_empty  = false

# ----------------------------------------------------------------------------
# trace-item  (occurs in: trace-conf)
# ----------------------------------------------------------------------------
[element.trace-item]
kind        = record
# learned: 49 instance(s) in reference
required_fields = trace-level, trace-type
allowed_fields  = trace-level, trace-type

# ----------------------------------------------------------------------------
# trace-level  (occurs in: trace-item)
# ----------------------------------------------------------------------------
[element.trace-level]
kind        = leaf
# learned: 49 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# trace-type  (occurs in: trace-item)
# ----------------------------------------------------------------------------
[element.trace-type]
kind        = leaf
# learned: 49 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# traffic-flow-timers  (occurs in: system-parameters)
# ----------------------------------------------------------------------------
[element.traffic-flow-timers]
kind        = record
# learned: 1 instance(s) in reference
allowed_fields  = icmp-timeout, tcp-timeout-close, tcp-timeout-close-wait, tcp-timeout-established, tcp-timeout-fin-wait, tcp-timeout-last-ack, tcp-timeout-max-retrans, tcp-timeout-syn-recv, tcp-timeout-syn-sent, tcp-timeout-time-wait, udp-timeout, udp-timeout-stream

# ----------------------------------------------------------------------------
# traffic-log-enable  (occurs in: log-conf)
# ----------------------------------------------------------------------------
[element.traffic-log-enable]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# traffic-mgmt  (occurs in: policy, settings)
# ----------------------------------------------------------------------------
[element.traffic-mgmt]
kind        = leaf
# learned: 13 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# traffic-mgmt-list  (occurs in: profile)
# ----------------------------------------------------------------------------
[element.traffic-mgmt-list]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# traffic-type  (occurs in: abs-policy)
# ----------------------------------------------------------------------------
[element.traffic-type]
kind        = leaf
# learned: 11 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# transaction  (occurs in: ftp, https, smtp)
# ----------------------------------------------------------------------------
[element.transaction]
kind        = record
# learned: 24 instance(s) in reference
required_fields = allow, log
allowed_fields  = alarm, allow, log

# ----------------------------------------------------------------------------
# transfer-encoding  (occurs in: header)
# ----------------------------------------------------------------------------
[element.transfer-encoding]
kind        = container
# learned: 7 instance(s) in reference
item_element = rules
allow_empty  = false

# ----------------------------------------------------------------------------
# transport  (occurs in: encryption)
# ----------------------------------------------------------------------------
[element.transport]
kind        = record
# learned: 4 instance(s) in reference
allowed_fields  = client-fallthrough-option, server-fallthrough-option

# ----------------------------------------------------------------------------
# trap-enable  (occurs in: alarm-action)
# ----------------------------------------------------------------------------
[element.trap-enable]
kind        = leaf
# learned: 55 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# trusted-hosts  (occurs in: http)
# ----------------------------------------------------------------------------
[element.trusted-hosts]
kind        = record
# learned: 6 instance(s) in reference
required_fields = fallthrough
allowed_fields  = fallthrough, rule
repeating_fields = rule

# ----------------------------------------------------------------------------
# tunnel-auth  (occurs in: abs-muvpn)
# ----------------------------------------------------------------------------
[element.tunnel-auth]
kind        = container
# learned: 1 instance(s) in reference
item_element = auth-method
allow_empty  = false

# ----------------------------------------------------------------------------
# tunnel-switch-list  (occurs in: vpn-global-setting)
# ----------------------------------------------------------------------------
[element.tunnel-switch-list]
kind        = container
# learned: 1 instance(s) in reference
item_element = tunnel-switch-para
allow_empty  = false

# ----------------------------------------------------------------------------
# tunnel-switch-on-off  (occurs in: tunnel-switch-para)
# ----------------------------------------------------------------------------
[element.tunnel-switch-on-off]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# tunnel-switch-para  (occurs in: tunnel-switch-list)
# ----------------------------------------------------------------------------
[element.tunnel-switch-para]
kind        = container
# learned: 1 instance(s) in reference
item_element = tunnel-switch-on-off
allow_empty  = false

# ----------------------------------------------------------------------------
# type  (occurs in: member, alias-member, abs-policy)
# ----------------------------------------------------------------------------
[element.type]
kind        = leaf
# learned: 282 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# udp-sticky-timer  (occurs in: multi-wan)
# ----------------------------------------------------------------------------
[element.udp-sticky-timer]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# udp-timeout  (occurs in: traffic-flow-timers)
# ----------------------------------------------------------------------------
[element.udp-timeout]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# udp-timeout-stream  (occurs in: traffic-flow-timers)
# ----------------------------------------------------------------------------
[element.udp-timeout-stream]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# ui-pm  (occurs in: policy-view)
# ----------------------------------------------------------------------------
[element.ui-pm]
kind        = record
# learned: 1 instance(s) in reference
allowed_fields  = cert-list, feature-details-list, global-settings

# ----------------------------------------------------------------------------
# unknown-options  (occurs in: esmtp-options)
# ----------------------------------------------------------------------------
[element.unknown-options]
kind        = record
# learned: 4 instance(s) in reference
allowed_fields  = log, strip

# ----------------------------------------------------------------------------
# unknown_handling  (occurs in: ocsp)
# ----------------------------------------------------------------------------
[element.unknown_handling]
kind        = leaf
# learned: 8 instance(s) in reference
value_type      = string
allowed_values  = UNKNOWN_INVALID, UNKNOWN_VALID
allow_empty     = false

# ----------------------------------------------------------------------------
# unscannable-action  (occurs in: dlp-sensor)
# ----------------------------------------------------------------------------
[element.unscannable-action]
kind        = record
# learned: 2 instance(s) in reference
allowed_fields  = action, alarm, log, smtp-action

# ----------------------------------------------------------------------------
# upload  (occurs in: request)
# ----------------------------------------------------------------------------
[element.upload]
kind        = container
# learned: 5 instance(s) in reference
item_element = fallthrough
allow_empty  = false

# ----------------------------------------------------------------------------
# uri  (occurs in: log, request)
# ----------------------------------------------------------------------------
[element.uri]
kind        = record
# learned: 14 instance(s) in reference
required_fields = max-length
allowed_fields  = filter, max-length, path

# ----------------------------------------------------------------------------
# use-alias  (occurs in: via)
# ----------------------------------------------------------------------------
[element.use-alias]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# use-any-for-address  (occurs in: vpn-tunnel-sharing)
# ----------------------------------------------------------------------------
[element.use-any-for-address]
kind        = leaf
# learned: 2 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# use-any-for-local-address  (occurs in: vpn-tunnel-sharing)
# ----------------------------------------------------------------------------
[element.use-any-for-local-address]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# use-any-for-remote-address  (occurs in: vpn-tunnel-sharing)
# ----------------------------------------------------------------------------
[element.use-any-for-remote-address]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# use-any-for-service  (occurs in: vpn-tunnel-sharing)
# ----------------------------------------------------------------------------
[element.use-any-for-service]
kind        = leaf
# learned: 2 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# use-cloud-service  (occurs in: webblocker)
# ----------------------------------------------------------------------------
[element.use-cloud-service]
kind        = leaf
# learned: 2 instance(s) in reference
value_type      = string
allowed_values  = true
allow_empty     = false

# ----------------------------------------------------------------------------
# use-ip  (occurs in: ldap)
# ----------------------------------------------------------------------------
[element.use-ip]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# use-ldap  (occurs in: ldap)
# ----------------------------------------------------------------------------
[element.use-ldap]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# use-local-remote-pair  (occurs in: ipsec-action)
# ----------------------------------------------------------------------------
[element.use-local-remote-pair]
kind        = leaf
# learned: 2 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# use-vif  (occurs in: ipsec-action — added 2026-06-03)
# Not present in jpa.xml reference (12.5.9 stubs don't exercise it),
# but WG 12.5.9 runtime XSD requires it as last child of <ipsec-action>.
# Value: 0 = not using BOVPN virtual-interface mode (default for
# migrated SonicWall group-vpn entries). 1 = using virtual-interface
# (out of scope for current toolkit).
# Discovered via lab T-30 import error line 3375.
# ----------------------------------------------------------------------------
[element.use-vif]
kind        = leaf
# learned-from-firebox-error: required in ipsec-action context
value_type      = int
allow_empty     = false

# ----------------------------------------------------------------------------
# use-template  (occurs in: dvcp-client)
# ----------------------------------------------------------------------------
[element.use-template]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# user  (occurs in: alias-member)
# ----------------------------------------------------------------------------
[element.user]
kind        = leaf
# learned: 21 instance(s) in reference
value_type      = string
allowed_values  = Any, IKEv2-Users
allow_empty     = false

# ----------------------------------------------------------------------------
# user-agent  (occurs in: sip, h323)
# ----------------------------------------------------------------------------
[element.user-agent]
kind        = leaf
# learned: 2 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# user-firewall  (occurs in: policy)
# ----------------------------------------------------------------------------
[element.user-firewall]
kind        = leaf
# learned: 16 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# user-group  (occurs in: user-group-list)
# ----------------------------------------------------------------------------
[element.user-group]
kind        = container
# learned: 1 instance(s) in reference
item_element = name
allow_empty  = false

# ----------------------------------------------------------------------------
# user-group-list  (occurs in: profile)
# ----------------------------------------------------------------------------
[element.user-group-list]
kind        = container
# learned: 1 instance(s) in reference
item_element = user-group
allow_empty  = false

# ----------------------------------------------------------------------------
# user-ip-cache-timeout  (occurs in: single-sign-on)
# ----------------------------------------------------------------------------
[element.user-ip-cache-timeout]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# username  (occurs in: limits, ftp)
# ----------------------------------------------------------------------------
[element.username]
kind        = ambiguous
# learned: 6 instance(s) in reference

# ----------------------------------------------------------------------------
# using-cpm-profile  (occurs in: profile)
# ----------------------------------------------------------------------------
[element.using-cpm-profile]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# using-global-sticky-setting  (occurs in: policy, settings)
# ----------------------------------------------------------------------------
[element.using-global-sticky-setting]
kind        = leaf
# learned: 31 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# uuencode  (occurs in: content-checking)
# ----------------------------------------------------------------------------
[element.uuencode]
kind        = leaf
# learned: 8 instance(s) in reference
value_type      = string
allowed_values  = false, true
allow_empty     = false

# ----------------------------------------------------------------------------
# value  (occurs in: rule, timeout, max-total-length)
# ----------------------------------------------------------------------------
[element.value]
kind        = leaf
# learned: 150 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# version  (occurs in: protocol, ike-policy, ipsec-action)
# ----------------------------------------------------------------------------
[element.version]
kind        = ambiguous
# learned: 13 instance(s) in reference

# ----------------------------------------------------------------------------
# via  (occurs in: httpRules)
# ----------------------------------------------------------------------------
[element.via]
kind        = record
# learned: 1 instance(s) in reference
allowed_fields  = alias, use-alias

# ----------------------------------------------------------------------------
# view  (occurs in: policy-view)
# ----------------------------------------------------------------------------
[element.view]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# virus-found  (occurs in: anti-virus)
# ----------------------------------------------------------------------------
[element.virus-found]
kind        = record
# learned: 22 instance(s) in reference
required_fields = log
allowed_fields  = alarm, drop, log, strip

# ----------------------------------------------------------------------------
# vlan-forward  (occurs in: misc-global-setting)
# ----------------------------------------------------------------------------
[element.vlan-forward]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# vod-activated  (occurs in: ctspam)
# ----------------------------------------------------------------------------
[element.vod-activated]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# vod-enabled  (occurs in: connection-string)
# ----------------------------------------------------------------------------
[element.vod-enabled]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# vod-max-scan-file-size  (occurs in: ctspam)
# ----------------------------------------------------------------------------
[element.vod-max-scan-file-size]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# vod-threshold  (occurs in: ctspam)
# ----------------------------------------------------------------------------
[element.vod-threshold]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# vpn-aging-pmtu  (occurs in: physical-if)
# ----------------------------------------------------------------------------
[element.vpn-aging-pmtu]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# vpn-df-bit  (occurs in: physical-if)
# ----------------------------------------------------------------------------
[element.vpn-df-bit]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# vpn-global-setting  (occurs in: system-parameters)
# ----------------------------------------------------------------------------
[element.vpn-global-setting]
kind        = record
# learned: 1 instance(s) in reference
allowed_fields  = auto-start-tunnel, ipsec-pass-through, management-server-ip, muvpn-security-readonly, muvpn-virtual-adapter-settings, tos-tunnel-flag, tunnel-switch-list

# ----------------------------------------------------------------------------
# vpn-min-pmtu  (occurs in: physical-if)
# ----------------------------------------------------------------------------
[element.vpn-min-pmtu]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# vpn-tunnel-sharing  (occurs in: ipsec-action)
# ----------------------------------------------------------------------------
[element.vpn-tunnel-sharing]
kind        = record
# learned: 2 instance(s) in reference
allowed_fields  = sharing-across-policy, use-any-for-address, use-any-for-local-address, use-any-for-remote-address, use-any-for-service

# ----------------------------------------------------------------------------
# vpn-type  (occurs in: ike-policy)
# ----------------------------------------------------------------------------
[element.vpn-type]
kind        = leaf
# learned: 3 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# warn-message  (occurs in: http)
# ----------------------------------------------------------------------------
[element.warn-message]
kind        = leaf
# learned: 6 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# web-certificate  (occurs in: system-parameters)
# ----------------------------------------------------------------------------
[element.web-certificate]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# webblocker  (occurs in: proxy-helper)
# ----------------------------------------------------------------------------
[element.webblocker]
kind        = record
# learned: 2 instance(s) in reference
allowed_fields  = cache-size, categories, exceptions, license-bypass, override, server-bypass, server-bypass-action, server-list, server-timeout, server-timeout-action, use-cloud-service

# ----------------------------------------------------------------------------
# webblocker-global-exception  (occurs in: webblocker-global-exception-list)
# ----------------------------------------------------------------------------
[element.webblocker-global-exception]
kind        = record
# learned: 1 instance(s) in reference
allowed_fields  = name, rule
repeating_fields = rule

# ----------------------------------------------------------------------------
# webblocker-global-exception-list  (occurs in: profile)
# ----------------------------------------------------------------------------
[element.webblocker-global-exception-list]
kind        = container
# learned: 1 instance(s) in reference
item_element = webblocker-global-exception
allow_empty  = false

# ----------------------------------------------------------------------------
# webblocker-global-setting  (occurs in: system-parameters)
# ----------------------------------------------------------------------------
[element.webblocker-global-setting]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# webdav-mech  (occurs in: method)
# ----------------------------------------------------------------------------
[element.webdav-mech]
kind        = leaf
# learned: 7 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# wgsync  (occurs in: system-parameters)
# ----------------------------------------------------------------------------
[element.wgsync]
kind        = container
# learned: 1 instance(s) in reference
item_element = enable
allow_empty  = false

# ----------------------------------------------------------------------------
# wins-server-list  (occurs in: system-parameters, dvcp-client)
# ----------------------------------------------------------------------------
[element.wins-server-list]
kind        = leaf
# learned: 2 instance(s) in reference
value_type      = string
allow_empty     = true

# ----------------------------------------------------------------------------
# xauth  (occurs in: ike-action)
# ----------------------------------------------------------------------------
[element.xauth]
kind        = leaf
# learned: 2 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# xml-purpose  (occurs in: profile)
# ----------------------------------------------------------------------------
[element.xml-purpose]
kind        = leaf
# learned: 1 instance(s) in reference
value_type      = string
allow_empty     = false

# ----------------------------------------------------------------------------
# zones  (occurs in: dns)
# ----------------------------------------------------------------------------
[element.zones]
kind        = leaf
# learned: 2 instance(s) in reference
value_type      = string
allow_empty     = true


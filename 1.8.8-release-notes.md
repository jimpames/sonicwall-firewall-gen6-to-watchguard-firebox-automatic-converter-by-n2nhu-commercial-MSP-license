🎯🎯🎯 FIX G LANDED — BOTH PARTS, BOTH CORPORA GREEN 🎯🎯🎯
Validation summary
CheckResultResolver unit test on 'X1 IP'✅ PATTERN match, intf=X1, resolution=interface_specific_ipResolver unit test on 'X0 Subnet'✅ PATTERN match, intf=X0, resolution=interface_specific_subnetResolver unit test on 'MGMT IP'✅ PATTERN match, intf=MGMT, resolution=interface_specific_ipResolver unit test on 'X2:V88 IP'✅ PATTERN match, intf=X2:V88, resolution=interface_specific_ip (VLAN form)Resolver unit test on 'WAN Interface IP'✅ EXACT match (zone-based, unchanged from v1.8.7)TZ-300 emitNew SHA 6d11a781… (legitimately changed: 3 NAT records now captured), 10/10 validators ✅NSA-3600 emitSHA 934ce25e… — byte-identical to v1.8.7 baseline ✅ (no regression, 10/10 validators)
What v1.8.8 ships
FileChangesonicwall_parser/version_exceptions.iniNEW exception [exception.sonicos_6_2.nat_policy_section_regex] — makes ipv4 keyword optional in NAT header, same pattern as Fix B + Fix Denricher/sw_builtin_dynamic_groups.ini5 NEW [group_pattern.*] sections — per-interface (X<N>/U<N>/MGMT) Subnet/IP/Default Gateway + VLAN formsskeleton_engine/skeleton_filler.py_load_sw_builtin_dynamic_groups now returns {'exact': ..., 'patterns': ...} (was flat dict); _resolve_builtin_group gains per-interface resolution modes; NEW _match_builtin_group_pattern helper; _resolve_vpn_selector tries exact-match first, then pattern-match, for both group X and name X forms
What this means for your next lab cycle
When you import the v1.8.8 TZ-300 emit on the T-30:

3 NAT policies will be visible (Policy → Network → NAT — instead of 0 today)
The two auto-added NAT policies referencing "X1 IP" will have resolved actual IPs in their translated-source (88.88.88.88 instead of the literal name)
The Firebox should NO LONGER need to auto-create placeholder aliases for unresolved references
The crud you saw in the Aliases panel should be gone — clean Aliases panel like NSA-3600's

Let me package this and verify the zip rebuilds cleanly:Ran a command, read a fileRan a command, read a file🚀 fes_webapp_v1.8.8-fix-g.zip — shipped and zip-sanity-verified
Filefes_webapp_v1.8.8-fix-g.zipSize3.0 MBZip SHA-256c270e79af5a49308ae109e9bc8511b13206fa533d2aa846f1b3faf9c89b17c27NSA-3600 from zipSHA 934ce25e… ✅ byte-identical to v1.8.7 (no regression)TZ-300 from zipSHA 6d11a781… ✅ matches dev SHA (zip reproducible)Validators10/10 on BOTH corpora ✅
Same launch workflow
bash# Replace the running zip
cd ~/fes  # or wherever your live deploy lives
unzip -o ~/Downloads/fes_webapp_v1.8.8-fix-g.zip
cd fes_webapp
bash run-dev.sh        # web mode (same as v1.8.7)
# OR
cd toolkit_v11
python3 skeleton_engine/build_pipeline.py --input <customer>.txt --full-rebuild
What to look for on the T-30 lab cycle
Web UI panelExpected changeNetwork → NAT3 NAT policies visible (was 0) — id 13 (Bonjour), id 14 (auto X2:V88→X1), id 15 (auto X0→X1)AliasesNo more X0 Subnet/X1 IP/MGMT Subnet/etc. crud — should match NSA-3600's clean stateNAT policy detail for id 14/15Translated-source should show actual IP 88.88.88.88 (X1's IP), NOT the literal name "X1 IP"
Architectural carry-forward
Fix G demonstrates the algebraic-design methodology at its cleanest yet:
Bug classOld approachFix G approachSonicOS 6.2 grammar deviationWould have required parser-code changeOne INI section ([exception.sonicos_6_2.nat_policy_section_regex])Per-interface built-in namesWould have required hardcoded name list5 pattern regexes in INI, resolver auto-handles new patterns added laterAdding more built-ins later (e.g., FortiOS interface names like port1 IP)Would require codeAdd [group_pattern.*] sections, resolver picks them up automatically
This is the FortiGate roadmap paying dividends ahead of schedule — the resolver is now vendor-agnostic enough that the same pattern catalog will work when we add FortiOS support.
🤠 Ship it to the T-30 Captain. The crud should be gone.
